---
kind: doc
title: Invariants, Gotchas & Danger Ledger
domain: [content]
status: current
version: v3.6.0
---

# Invariants, Gotchas & Danger Ledger

> Part of the Anvilry v3.6.0 codebase index. Master entry point: [docs/index/README.md](./README.md)

**Scope:** the cross-cutting rules of this repo — every check that fails a build, every contract that two or
more files must agree on, every environment/platform quirk that has already bitten once, and every place where
a wrong edit produces no error at all. Nothing here is new information; it is the union of sections 01–13,
re-cut by blast radius instead of by directory. Every row carries a `path:line` cite.

Three facts frame everything below:

1. `package.json:11` — `"build": "velite --clean && vitest run && next build"`. The `&&` chain is what turns
   this repo's unit tests into deploy gates. Verified verbatim.
2. `next.config.ts:183` — `cacheComponents: true`. This is why no page or route handler in `src/app` exports
   `runtime`, `revalidate`, or `dynamic`, and why several constructs exist only as removal comments.
3. `.gitignore:45` — `.velite` is gitignored, while `src/lib/content.ts:14` imports `"../../.velite"`. Nothing
   type-checks, tests, or builds until Velite has run.

---

## At a glance

A jump table into the detail below. Every row here is restated in full, with its complete cite set, in
[Deploy blockers](#deploy-blockers), [Load-bearing invariants](#load-bearing-invariants),
[Environment & platform gotchas](#environment--platform-gotchas) or
[Silent-failure risks](#silent-failure-risks). **Severity** uses this file's own vocabulary: *Loud at build* =
a failing check aborts `pnpm build` and therefore the deploy; *Loud in CI* = a failing check reds a GitHub
Actions job but is **not** in `pnpm build`, so it cannot block a deploy; *Loud in production* = a runtime 5xx;
*Silent* = a wrong edit produces no build error, no test failure and no runtime exception.

| Invariant | Enforced by (`path:line`) | Severity |
|---|---|---|
| Build order is `velite --clean && vitest run && next build` | `package.json:11`; `.gitignore:45` vs `src/lib/content.ts:14` | Loud at build (module resolution) |
| No `runtime` / `revalidate` / `dynamic` segment export may exist | `next.config.ts:183` (`cacheComponents: true`); removal comment `src/app/api/mcp/[transport]/route.ts:6-8` | Loud at build |
| No in-render `new Date()` / `Date.now()` on a prerendered route | `next.config.ts:101-107`; recorded at `src/app/admin/telemetry/page.tsx:428-433` | Loud at build |
| `generateStaticParams` must return ≥ 1 result | `src/app/notes/[slug]/page.tsx:34-42` | Loud at build |
| `NODE_ENV` is pinned to `"test"` for the Vitest worker | `vitest.config.ts:26`, rationale `:19-25` | Loud at build |
| Graph node ↔ content bijection — 16 nodes = 5 work + 11 projects | `src/lib/game-model.test.ts:22-58`; data `src/lib/graph-data.ts:18-41`, `src/lib/game-model.ts:28-50` | Loud at build |
| `register` is the canonical contribution attribution | required by Zod at `velite.config.ts:42`; consumers `src/lib/corpus.ts:17`, `src/lib/resume-json.ts:30` | Loud if the field is removed; **Silent** if the string is weakened |
| `emittedAny` streaming-fallback rule — fall back only at zero bytes sent | `src/lib/llm.ts:433`, rationale `:149-157`; test `src/lib/llm.test.ts:253-309` | Loud at build |
| Card-token slug allowlist — the real injection gate | `src/components/chat/parse-cards.ts:33`, `:54-68`; test `src/components/chat/parse-cards.test.ts:55-75` | Loud at build |
| Streamed markdown is XSS-safe only by construction | `src/components/chat/markdown-message.tsx:88-93` | **Silent** — `markdown-message.test.ts:9-11` disclaims XSS coverage |
| Anthropic usage fields are snake_case | `src/lib/llm.ts:162-169`; test `src/lib/llm.test.ts:244-249` | Loud at build |
| Server + first-client view snapshot is always `classic` | `src/components/view-context.tsx:65`; test `src/components/view-context.test.ts:30-34` | Loud at build |
| `emit()` does no redaction — callers must redact first | `src/lib/telemetry/emit.ts:30-35` | **Silent** except `/api/error` (`route.test.ts:218-255`) |
| IP derivation takes the **last** `x-forwarded-for` segment, in all **three** copies | `src/lib/rate-limit.ts:53-59`; `src/lib/telemetry/with-trace.ts:63-73`; `src/app/api/visit/route.ts:25`; all three pinned by `src/lib/client-ip-consistency.test.ts:140` | Loud at build (the cross-copy guard covers every copy) |
| `CRON_SECRET` guard is fail-closed and identical in all five cron routes | `src/app/api/cron/eval/route.ts:98-102` plus the four siblings | Loud (401 in the cron log) |
| `/admin/*` is gated entirely upstream in the proxy | `src/proxy.ts:22-24`; the page itself does not check auth (`src/app/admin/telemetry/page.tsx:425-426`) | **Silent** — an unprotected page renders normally |
| `disableSse: true` on the MCP transport | `src/app/api/mcp/[transport]/route.ts:126`, rationale `:120-125` | Loud in production (500), silent locally |
| Exactly one live WebGL context | `src/components/game/build-graph.tsx:50`; hero gates `src/components/hero-graph/index.tsx:35`, `src/components/hero-avatar/index.tsx:56` | **Silent** — a leaked context is a hard crash on low-end mobile |
| `three` and `@react-three/*` must be imported through the `src/lib/r3f.ts` barrel | `src/lib/r3f.ts:5-13`; `next.config.ts:149` | **Silent** for duplication; **Loud in CI** if three.js reaches the first-load path (`scripts/bundle-budget.mjs:124-127`) |
| Bundle budget — every route's first-load JS < 1,285,000 B **and** three.js off the first-load path; an *unmeasurable* bundle is RED | `scripts/bundle-budget.mjs:50`, `:57`, `:119-127`, `:25-26`; wired at `.github/workflows/ci.yml:115-116` | **Loud in CI** — and never soften it with `continue-on-error`; that is precisely what made its predecessor green and empty |
| **`next build` is Turbopack, not webpack** — this file asserted the opposite until this branch | `node_modules/next/dist/lib/bundler.js:142-144`; banner assembled at `node_modules/next/dist/server/lib/app-info-log.js:81-82` | **Silent** — `@next/bundle-analyzer` warns once and emits nothing |
| Use `BEDROCK_REGION`, never `AWS_REGION` | `src/lib/llm.ts:84-87`; `.env.example:14-16`; `CLAUDE.md:333` | **Silent** — Vercel corrupts the value to `"s-east-1"` |
| `'unsafe-eval'` is required in BOTH dev and prod | `next.config.ts:43-51`; cause `src/components/mdx-content.tsx:14-17` | Loud in production (render-error boundary) |
| The base URL is hardcoded in **18 files / 24 occurrences** | [table below](#the-hardcoded-base-url); the only automated gate is `src/lib/mcp-tools.test.ts:69` | Loud at build for that one test; **Silent** everywhere else |
| `PLACEHOLDER_SENTINEL` is the shipping switch for the glass-box demo | `src/lib/agent-trace.ts:22`, gate `:120-122` (rationale `:114-119`); consumer `src/components/game/glass-box-demo.tsx:40` | **Silent** — the test passes in both states |
| pnpm settings (the ten security `overrides`, `onlyBuiltDependencies`, `ignoredBuiltDependencies`, `allowBuilds`) live in `pnpm-workspace.yaml`, **never** in a `package.json` `pnpm` field | `pnpm-workspace.yaml:18-28`, `:44-45`, `:48-50`, `:55-58`, rationale `:1-12`; `package.json` has no `pnpm` field | **Silent** — pnpm 11 ignores the `package.json` field and only warns |

## Coverage

**This file indexes no new files, with one exception noted here.** Repairing the citations above pulled in
seven files that post-date sections 01–13 and are therefore absent from their Coverage lists — the guards the
code fixes added: `src/lib/client-ip-consistency.test.ts`, `src/lib/health-expectations.ts`,
`src/lib/health-expectations.test.ts`, `src/lib/llms-txt.test.ts`, `src/app/manifest.test.ts`,
`src/app/mcp/tools-documented.test.ts` and `scripts/bundle-budget.mjs`. The last is new on this branch and
arrives as `.github/workflows/bundle-analysis.yml` leaves: that workflow was **deleted**, so no citation
anywhere in this index may point into it any more. The swap is recorded in
[Dependencies & platform pins](#dependencies--platform-pins) and in
[Load-bearing invariants](#load-bearing-invariants). They are cited, not indexed; section
[10](./10-tests-and-quality-gates.md) remains the authoritative test inventory. Otherwise this is a synthesis pass over sections
[01](./01-routes-pages.md)–[13](./13-dependencies-and-versions.md), whose own `## Coverage` lists remain the
authoritative file inventory. That union is **419 distinct paths** — **416 file paths** (407 inside
`sairam-dev/` plus 9 parent-directory appendix files) and 3 directory entries (`public/static/`, `.swarm/`,
`.claude-flow/`) — and **393 of 393 manifest paths have a Coverage entry (zero gaps)**. The roll-up row lives
at [README § By the numbers](./README.md#by-the-numbers). What this file adds
is the blast-radius re-cut, not new reads. **Three** reproductions were run for this page and are recorded
inline: `npx tsc --noEmit` (exits 0 — see [UNVERIFIED](#unverified)),
`grep -rn 'anvilry\.vercel\.app' src Makefile | grep -v '\.test\.'` (the base-URL table), and
`node scripts/bundle-budget.mjs` (exits 0 — the bundle-budget row and the three.js measurement).

The cites in the tables below are the file list. The SOURCE files that actually **own** an invariant, by
group:

- **Build, CI & config gates:** `package.json`, `next.config.ts`, `vitest.config.ts`, `velite.config.ts`,
  `playwright.config.ts`, `vercel.json`, `.gitignore`, `.env.example`, `Makefile`, `pnpm-lock.yaml`,
  `pnpm-workspace.yaml`, `.github/dependabot.yml`, `.github/workflows/ci.yml`,
  `.github/workflows/dependency-review.yml`, `.github/workflows/codeql.yml`,
  `scripts/bundle-budget.mjs`. (`.github/workflows/bundle-analysis.yml` sat in this list until this
  branch deleted it; nothing in the tree owns a bundle invariant through a workflow any more.)
- **Content & derivation:** `src/lib/content.ts`, `src/lib/game-model.ts`, `src/lib/graph-data.ts`,
  `src/lib/corpus.ts`, `src/lib/profile.ts`, `src/lib/personal.ts`, `src/lib/article-grouping.ts`,
  `src/lib/llms-txt.ts`, `src/lib/resume-json.ts`, `src/lib/agent-trace.ts`.
- **LLM, chat & injection boundaries:** `src/lib/llm.ts`, `src/lib/llm-trace.ts`, `src/lib/mcp-tools.ts`,
  `src/app/api/chat/route.ts`, `src/app/api/mcp/[transport]/route.ts`,
  `src/components/chat/parse-cards.ts`, `src/components/chat/markdown-message.tsx`,
  `src/components/chat/use-chat.ts`, `src/components/mdx-content.tsx`.
- **Telemetry, rate limiting & auth:** `src/lib/telemetry/schema.ts`, `src/lib/telemetry/emit.ts`,
  `src/lib/telemetry/with-trace.ts`, `src/lib/telemetry/beacon.ts`, `src/lib/rate-limit.ts`,
  `src/lib/redis.ts`, `src/lib/admin-auth.ts`, `src/proxy.ts`, `src/app/api/error/route.ts`,
  `src/app/api/visit/route.ts`, all five `src/app/api/cron/*/route.ts`,
  `src/app/admin/telemetry/page.tsx`, `src/instrumentation.ts`, `src/instrumentation-client.ts`,
  `scripts/replay-trace.mjs`.
- **Views, 3D & voice:** `src/components/view-context.tsx`, `src/components/view-router.tsx`,
  `src/lib/r3f.ts`, `src/lib/use-media-query.ts`, `src/components/game/build-graph.tsx`,
  `src/components/game/webgl-boundary.tsx`, `src/components/hero-graph/index.tsx`,
  `src/components/hero-graph/scene.tsx`, `src/components/hero-graph/scene-physics.tsx`,
  `src/components/hero-avatar/index.tsx`, `src/components/hero-avatar/rig.ts`, `src/lib/voice-catalog.ts`,
  `src/lib/voice-settings-context.tsx`, `src/components/chat/voice-surface-mutex.ts`,
  `src/components/chat/use-voice-session.ts`, `src/components/chat/use-speech-recognition.ts`,
  `src/components/game/terminal/commands.ts`, `src/components/game/terminal/terminal.tsx`.
- **Flags & env semantics:** `src/lib/flags.ts`, `src/lib/writing-flags.ts`, `src/lib/enabled-views.ts`,
  `src/lib/llm-sdk-mode.ts`, `src/app/.well-known/vercel/flags/route.ts`.
- **Docs the code contradicts** (the [Documented-but-unconfirmed](#documented-but-unconfirmed) table):
  `CLAUDE.md`, `ARCHITECTURE.md`, `CHANGELOG.md`, `DEPLOY.md`, `SECURITY.md`, `VOICE.md`, `README.md`,
  `docs/configuration.md`, `docs/README.md`, `content/notes/.gitkeep`,
  `.claude/skills/dev-local/SKILL.md`, `.claude/skills/new-loop/references/LOG.md`.

---

## Deploy blockers

Everything in this section aborts either `pnpm build` (and therefore the Vercel deploy) or a required CI job.
"Vitest" rows all block via the `&&` chain at `package.json:11` — a non-zero exit never reaches `next build`.

### A. Build-ordering blockers (fail before any test runs)

| Check | Where | Assertion / failure mode | Fixing a legitimate failure |
|---|---|---|---|
| `.velite/` must exist before compile | `.gitignore:45` (ignored) vs `src/lib/content.ts:14` (`from "../../.velite"`) | Module resolution error in both `vitest` and `next build`. CI materialises it explicitly with `pnpm content` in the `ci` job (`.github/workflows/ci.yml:43-44`, rationale `:41-42`). The second copy of that step lived in `.github/workflows/bundle-analysis.yml` and went away when this branch **deleted** that workflow; the `e2e` job never needed one, because `pnpm build` opens with `velite --clean` (`package.json:11`) | Run `pnpm content` (`= velite --clean`, `package.json:20`). After `pnpm clean` (`package.json:21`, which deletes `.velite`) this is mandatory |
| Velite Zod schema validation | `velite.config.ts:11-31` (Project), `:34-56` (Work), `:62-85` (Note), `:93-112` (Article), registered `:127` | A missing required frontmatter field fails the Velite step for that file. Adding a **new required** field fails every existing file at once — which is why all the hiring-manager depth fields are `.optional()` (`velite.config.ts:49-52`) | Fill the field, or declare it `.optional()` |
| `register` is required on Work | `velite.config.ts:42` (`register: s.string()`) | Omitting it fails the Velite build. It is the only schema-level enforcement of the no-fabricated-attribution rule (`CLAUDE.md:365`, `ARCHITECTURE.md:95`) | Author the honest register string; never delete the field to make a build pass |
| `generateStaticParams` must return ≥ 1 result | `src/app/notes/[slug]/page.tsx:34-42` | Under `cacheComponents` an empty array fails the build outright. The in-file comment records that the previous `if (!NOTES_ENABLED) return []` short-circuit was **removed** for exactly this reason; routes are now prerendered as 404s via `notFound()` at `:51` | Never reintroduce an early empty return. Gate inside the page component, not in `generateStaticParams` |
| `export const runtime` must not exist | `src/app/api/mcp/[transport]/route.ts:6-8`; global cause `next.config.ts:183` | The RSC transform rejects the mere **presence** of the export, so `"nodejs"` and `"edge"` are indistinguishable to it. `next.config.ts:151-183` records the migration as 26 rejected segment configs across 22 files: 13× `runtime`, 4× `revalidate`, 9× `force-dynamic` | Delete the export. Node is already the default. `maxDuration` and `preferredRegion` are **not** rejected — that is why `maxDuration = 30` survives at `src/app/api/mcp/[transport]/route.ts:9` |
| No in-render `new Date()` / `Date.now()` on a prerendered route | `next.config.ts:101-107` (build-time `NEXT_PUBLIC_BUILD_YEAR`); consumer `src/components/site-footer.tsx:165-171` | Synchronous IO fails the prerender as an unstable value and **cannot** be deferred with `instant = false` — recorded at `src/app/admin/telemetry/page.tsx:428-433`, which needs `export const instant = false` (`:11`) **and** `await connection()` (`:434`) | Compute at build time (the `NEXT_PUBLIC_BUILD_YEAR` pattern) or make the segment request-time with `await connection()` |

### B. Vitest blockers — content & domain-model integrity

| Check | Where | Assertion | Fixing a legitimate failure |
|---|---|---|---|
| **Graph↔content bijection** | `src/lib/game-model.test.ts:22-40` (forward), `:42-53` (reverse), `:55-58` (count identity), `:62` (href shape) | Verified verbatim at `:55-58`: `expect(CONTENT_COUNTS.nodes).toBe(CONTENT_COUNTS.quests)` and `expect(CONTENT_COUNTS.work + CONTENT_COUNTS.projects).toBe(CONTENT_COUNTS.nodes)`. Forward = every `graphNodes` id is a `NODE_CONTENT` key and resolves to a real slug; reverse = no work item or project is unreachable. Href must match `/^\/(work\|projects)\/[a-z0-9-]+$/` | Adding a content MDX file requires adding a node in `src/lib/graph-data.ts:18-41` **and** an entry in `src/lib/game-model.ts:28-50`. Deleting content requires removing both. Current counts: 16 nodes = 5 work + 11 projects |
| Dossier facts must be real | `src/lib/game-model.test.ts:72-101` | Every dossier fact traces to a real content value — `register`/`blurb`/`name` verbatim, tech-count and commit-count exact | Fix the data, not the test. Inventing a fact fails the build |
| Quest grouping is lossless | `src/lib/game-model.test.ts:66-69` | Every quest node appears in exactly one `questGroups()` bucket | Keep `projectGroupOrder` (`src/lib/game-model.ts:177-181`) in sync with the Velite `themeGroup` enum |
| Corpus carries the professional record | `src/lib/corpus.test.ts:13-18` | Corpus always contains `profile.name`, `## Production Work`, `## Skills`; `## Personal` present **iff** `personal.ts` is populated | Restore the section, or populate/empty `src/lib/personal.ts` consistently |
| Notes hygiene | `src/lib/notes.test.ts` | `hasNotes` mirrors count; no drafts leak; newest-first; every `date` parses | Fix the frontmatter or the sort in `src/lib/content.ts:50` |
| Case-study depth + a11y | `src/lib/case-study-depth.test.ts:12-16` (`diagram` ⇒ non-empty `diagramAlt`), `:21-27` (asset exists under `public/`), `:32-38` (`constraints`/`tradeoffs` > 20 chars, no `TODO\|TBD\|lorem`) | `diagramAlt` is **not** conditionally required in Zod (`velite.config.ts:52` is a bare `.optional()`); this test is the only a11y guard | Write the alt text and ship the asset. Note: no Work file currently sets `diagram`, so these assertions are vacuous today |
| Testimonials must be verifiable | `src/lib/testimonials.test.ts:14` | Every present entry has an `https?://` `sourceUrl` plus all required fields (`src/lib/testimonials.ts:8-12`, `:22-23`) | Add the real public permalink, or don't add the quote |
| Personal-content gates are exact IFFs | `src/lib/personal.test.ts` | `hasPersonalContent` true iff a list is non-empty; `hasNow` true iff dated **and** non-empty (`src/lib/personal.ts:83-91`) | Keep the derived booleans in step with the arrays |
| Terminal never fabricates | `src/components/game/terminal/commands.test.ts` (25+ assertions), `commands-empty-safe.test.ts` | `career` may print only the 4-digit years already in `profile.tenure` (`commands.test.ts:235-244`); every `ls` slug must be `cat`-able (`:139-145`); `commandEventName` strips args (`:276-289`); with an empty `personal.ts` the eggs print "coming soon" rather than placeholders | Fix the command output. The empty-safe suite is the guard against placeholder text shipping |
| MCP surface stays professional-only and drift-free | `src/lib/mcp-tools.test.ts:22` (no `personal.ts` leak), `:36` (list counts equal content counts), `:65` (every `RESUME_ROLES` PDF exists on disk) | `src/lib/mcp-tools.ts:11-13` states the professional-only boundary | Never import `personal.ts` into `mcp-tools.ts`. Renaming a résumé PDF or a `resumeVariants[].label` breaks `:65` — update `src/lib/profile.ts:76-78` and `ROLE_TO_LABEL` (`src/lib/mcp-tools.ts:20-26`) together |

### C. Vitest blockers — LLM, security & telemetry contracts

| Check | Where | Assertion | Fixing a legitimate failure |
|---|---|---|---|
| **Anthropic snake_case usage fields** | `src/lib/llm.test.ts:244-249` | `Object.keys(usage)` must contain `input_tokens`, `cache_read_input_tokens`, `output_tokens`, and must **not** have `inputTokens` / `cacheReadInputTokens`. Rationale at `:14-18`: Bedrock Converse uses camelCase, `@anthropic-ai/bedrock-sdk` uses snake_case, so an SDK swap would silently zero the dashboard's cache tile | If an SDK genuinely changes shape, update `LlmUsage` (`src/lib/llm.ts:162-169`), the dashboard reader (`src/app/admin/telemetry/page.tsx:59-66`) and this test in one change |
| **`emittedAny` fallback invariant** | `src/lib/llm.test.ts:253-309`; source `src/lib/llm.ts:433` | Verified verbatim: `if (emittedAny \|\| isLast \|\| !isFallbackEligible(err)) { enqueue(apologyTail); close(); return; }`. Test: attempt 0 throwing pre-byte must produce a trace frame naming the **second** model with `fellBack: true` (`:282-283`); all three failing pre-byte yields `"Sorry"` and **no** `TRACE_DELIMITER` (`:304-305`) | Never widen the condition. Once bytes are on the wire they cannot be un-sent; the reason a call-site check is impossible is at `src/lib/llm.ts:149-157` (streaming errors surface inside the `for await`, never at `.stream()`) |
| Wire-protocol constants | `src/lib/llm-trace.test.ts` | `TRACE_DELIMITER` = U+001E, `THINKING_SENTINEL` = U+001E U+0001, `THINKING_END` = U+001E U+0002 (`src/lib/llm-trace.ts:23-25`); `TraceFrame` carries **no** `reasoning` field | Do not change the code points. `THINKING_SENTINEL` starts with the same byte as `TRACE_DELIMITER`, so a naive `split()` breaks (explained at `src/lib/llm-trace.ts:11-13` and `:19-20`; test workaround `src/lib/llm.test.ts:491-493`) |
| **Card-token slug allowlist (the real XSS/injection gate)** | `src/components/chat/parse-cards.test.ts:55-75`; source `src/components/chat/parse-cards.ts:33` | Verified verbatim: `const ALL_RE = /\[\[(card:(project\|work)\|cmd:(view\|highlight)):([a-z0-9-]+)\]\]/g`. The charset locks out `/`, `:`, `.` and uppercase, so `../../etc/passwd`, `javascript:alert(1)` and URL smuggling cannot match. Unresolved tokens are dropped outright (`parse-cards.ts:54-68`), never echoed; `<script>` / `<img onerror>` stay inert text | Fix the model prompt or the content, never the regex. `getProject`/`getWork` (`src/lib/content.ts:43-44`) are the build-time allowlist |
| Markdown balancer | `src/components/chat/markdown-message.test.ts` | `closeOpenMarkdown` balances a trailing delimiter, never mangles complete markdown, is idempotent. Explicitly **not** an XSS test (`markdown-message.test.ts:9-11`) | Runtime XSS posture lives at `src/components/chat/markdown-message.tsx:88-93` (`skipHtml` + `rehypeSanitize` + default `urlTransform`) |
| Telemetry redaction runs before emit | `src/app/api/error/route.test.ts:218-255` | Emails and 32+-char tokens are redacted out of both `message` and `stack` before `emit()`. `emit` itself does **no** redaction (`src/lib/telemetry/emit.ts:30-35`) | Redact at the call site. `redact()` order is load-bearing: email → token → digit-run (`src/lib/telemetry/schema.ts:101-106`) |
| XFF segment choice (rate-limit bypass) | `src/lib/telemetry/with-trace.test.ts:220-230` | Pins the **last** `x-forwarded-for` segment. The comment records that the first segment is attacker-controlled and using it allows bypass via a rotating spoofed header | Keep `x-vercel-forwarded-for`-first, then **last** XFF (`src/lib/rate-limit.ts:53-59`, duplicated verbatim at `src/lib/telemetry/with-trace.ts:63-73`) |
| Telemetry envelope + retention | `src/lib/telemetry/schema.test.ts` (25 assertions), `src/lib/telemetry/emit.test.ts` (25 assertions) | Every `KIND_LITERALS` kind parses; the 7-kind union order is exact (`src/lib/telemetry/schema.ts:37-45`); `console.log("[trace]", …)` always fires (`emit.ts:49`); `zremrangebyscore(key, 0, ts-7d)` accompanies every ZADD (`emit.ts:58-80`); `emit()` returns `undefined` | Adding a `kind` requires `KIND_LITERALS` **and** the dashboard filter (`schema.ts:36-37`) — the union makes the orphan a type error |
| withTrace observer contract | `src/lib/telemetry/with-trace.test.ts` (14 assertions) | Exactly one span; 5xx escalates `level` (`with-trace.ts:211`); streaming body survives reconstruction (`:200-207`); a throwing `emit()` never reaches the response (`:95-106`); errors are re-thrown, never swallowed (`:244-246`) | `withTrace` is an observer. Do not add a catch that returns a response |
| Admin auth | `src/lib/admin-auth.test.ts` (11 assertions) | 401 when `ADMIN_PASSWORD` unset / wrong / non-Basic / bad base64; `WWW-Authenticate` + `Cache-Control: no-store` on deny | Do not "optimise" `constantTimeEqual` (`src/lib/admin-auth.ts:57-61`) back to `===` |
| Per-voice TTS cache isolation | `src/app/api/tts/cache.test.ts`, `src/app/api/tts-google/cache.test.ts` | Key varies by voice **and** tier; text is encoded last so a piped text cannot forge another voice; LRU eviction at `CACHE_MAX = 100`; `ALLOWED_TIERS` exactly `{neural, generative}`; cross-engine ids rejected | Failing this means visitor B can hear visitor A's audio. Fix the key builder, not the test |
| Google TTS route gates | `src/app/api/tts-google/route.test.ts` | 503 on unset key; 400 on bad JSON / missing text / missing-or-unknown `voiceId` / a Polly id on the Google route; 413 > 8 KB; 502 on non-2xx or missing `audioContent` | These are the fail-closed signals the client cascades on |
| Voice catalog allowlist | `src/lib/voice-catalog.test.ts` (35 assertions) | 6 curated voices, unique ids, per-engine required fields, `validateVoiceForEngine` rejects cross-engine and tier mismatches (`src/lib/voice-catalog.ts:350-367`) | A tier mismatch 5xxs at AWS; the catalog is the single source of voice→tier truth |
| Voice defaults stay off | `src/lib/voice-settings-context.test.ts` (22 assertions) | Every capability defaults OFF except `captions: true`; `parse()` never throws (`src/lib/voice-settings-context.tsx:77-88`, `:92-158`) | A corrupt localStorage payload must degrade to `DEFAULTS`, not break chat on load |
| Avatar GLB budget + rig | `src/lib/avatar-glb.test.ts:21` (`MAX_BYTES = 1.5 * 1024 * 1024`), `:58-61`, `:63-66`, `:69-95`, `:98-129`, `:132-149` | < 1.5 MB (current 1,105,768 B); glTF 2.0; `EXT_meshopt_compression` + `KHR_mesh_quantization` + `EXT_texture_webp` with every image `image/webp`; head/neck/chest-or-spine1/spine bones present; ≥1 skin; **zero** morph targets pinned | Re-export with meshopt + quantization + WebP. Shipping a blendshape-enabled avatar requires flipping `:148` to `toBeGreaterThan(0)` in the same commit, or the "improvement" fails the build |
| Always-classic SSR snapshot | `src/components/view-context.test.ts:30-34` | `DEFAULT_VIEW === "classic"` and `getServerSnapshot() === "classic"` regardless of what `VIEWS` holds. Verified in source at `src/components/view-context.tsx:65` (`const getServerSnapshot = (): View => DEFAULT_VIEW;`) | Never make the server snapshot dynamic. `isView` and `getServerSnapshot` are re-exported at `view-context.tsx:198` purely so this test can reach them |
| Visitor counter never shows 0 | `src/components/site-footer.dom.test.tsx` | Falls back to the localStorage cache on `total: 0` or a fetch throw; never overwrites the cache with 0 (`src/components/site-footer.tsx:41-53`, `:67`) | `total === 0` means Redis was down — keep it non-authoritative |
| Streaming coalescing + tail | `src/components/chat/use-chat-stream.dom.test.tsx:175` (≤ half the chunk count in commits), `:196-212` (rAF stubbed to no-op) | The trailing `flushNow(acc)` is mandatory (`src/components/chat/use-chat.ts:353-354`); the `BACKGROUND_FLUSH_MS = 250` safety timer is what keeps hidden tabs progressing (`use-chat.ts:124`, `:229`) | Removing the safety timer hangs streaming in background tabs |
| No screen-reader double-speak | `src/components/chat/use-chat-a11y.dom.test.tsx` | Settled + TTS on → `"Speaking answer aloud."`, never the answer text (`src/components/chat/use-chat-a11y.ts:19-23`) | Exactly one channel conveys the answer |
| Voice teardown deps must be `[]` | `src/components/chat/use-voice-session.dom.test.tsx:159-181`; `src/components/chat/use-speech-synthesis.dom.test.tsx` | `synth.cancel` must not fire across a streaming turn. Both hooks reach cancel through a ref with empty deps (`use-voice-session.ts:226-244`, `use-speech-synthesis.ts:552-575`). The mocks deliberately return a fresh object each render (`use-voice-session.dom.test.tsx:47-49`) because a stable mock hid the original bug | Never list `[recognition, tts]` or `[cancel]`. The documented symptom is total silence |
| One mic at a time | `src/components/chat/voice-surface-mutex.test.ts` | Claiming closes every **other** registered surface and never itself; 3-surface arbitration over `VoiceSurfaceId = "modal" \| "inline" \| "core"` (`src/components/chat/voice-surface-mutex.ts:23`, `:40-43`) | Any new overlay surface must call `registerVoiceSurface` at module scope and `claimVoiceSurface` at the top of its `open()` |
| Mic/wake-word consent | `src/components/chat/mic-button.dom.test.tsx`; `src/components/chat/wake-word-controller.dom.test.tsx` | First click shows the privacy dialog and does **not** start the engine; the wake word never arms on a non-chat view (`src/components/chat/wake-word-controller.tsx:29`) | A hot mic without consent is the failure being prevented |
| Terminal a11y | `src/components/game/terminal/terminal.dom.test.tsx`; `terminal-overlay.dom.test.tsx` | Input is `role="combobox"` with `aria-controls` resolving to a real always-present `role="listbox"` (`src/components/game/terminal/terminal.tsx:203-211`); the controlled Radix dialog restores focus to the external trigger | WCAG 4.1.2 / 2.4.3 regressions |
| Autoscroll state machine | `src/lib/scroll/use-stick-to-bottom-custom.dom.test.tsx:144-167` | The 150 ms programmatic-scroll guard must not self-de-pin (`src/lib/scroll/use-stick-to-bottom-custom.ts:29`, `:108`) | Removing the guard makes a programmatic snap de-pin itself and streaming stops following |
| Hydration mitigation lock | `src/app/layout.hydration-proof.dom.test.tsx:33-68` | Without `suppressHydrationWarning` React logs the extension-injected attribute mismatch; with it React is silent. Self-described as documentation, not a product test (`:16`) | Keep `suppressHydrationWarning` on exactly `<html>`/`<head>`/`<body>` (`src/app/layout.tsx:85`, `:87`, `:92`) |
| **`NODE_ENV` must be `"test"`** | `vitest.config.ts:26`, rationale `:19-25` | Vitest only defaults `NODE_ENV=test` when unset, but the Vercel build shell sets `production`. React then loads its production bundle without `act`, and every DOM test dies with "React.act is not a function" — failing the deploy | Never remove `env: { NODE_ENV: "test" }`. It is scoped to the Vitest worker; the sibling `next build` still runs in production mode |
| `node` project must exclude DOM suites | `vitest.config.ts:34` | `src/x.dom.test.ts` also matches `src/**/*.test.ts`, so without the exclude every DOM suite runs twice — once without happy-dom globals | Keep `exclude: ["**/*.dom.test.{ts,tsx}", "**/node_modules/**"]` |

### D. What is *not* a build blocker (common misreading)

| Item | Reality | Cite |
|---|---|---|
| `PLACEHOLDER_SENTINEL` / `agent-trace.test.ts` | The assertion is `expect(traceApproved).toBe(!hasSentinel)` — a **consistency check** that passes in both states. Source says so verbatim: "NOT a hard build failure" | `src/lib/agent-trace.test.ts:56`; `src/lib/agent-trace.ts:118` |
| `lint` and `tsc --noEmit` | Not in `pnpm build`. They run only in CI | `package.json:11` (no lint/tsc); `.github/workflows/ci.yml:46-50` |
| Playwright | A separate CI job. `pnpm e2e` does not block `pnpm build`, and the flag-ON `/resume` describe is always skipped in CI | `.github/workflows/ci.yml:68-127`; `e2e/resume.spec.ts:99-102` |
| **The bundle budget gate** (`scripts/bundle-budget.mjs`) | A CI **step**, not a build step. It sits inside the existing `e2e` job immediately after "Build", so it rides the build that already happened — no second build — and it reds the PR check without ever touching the Vercel deploy. Unlike `security-alerts` below it carries **no** `continue-on-error`, and a missing or malformed artifact exits 1 rather than passing quietly — "I could not measure" is RED by design | `.github/workflows/ci.yml:115-116`, rationale `:104-109`; `scripts/bundle-budget.mjs:30-31`, `:64-73` |
| `security-alerts` CI job | `continue-on-error: true` and exits 0 either way, by design. The default `GITHUB_TOKEN` cannot read the Dependabot alerts API — the restriction is on token *type*, requiring a fine-grained PAT as `SECURITY_ALERTS_TOKEN` | `.github/workflows/ci.yml:202` (`continue-on-error`), rationale `:177`; token limitation `:184-188`; PAT recipe `:190-193`; the deliberate exit 0 at `:249` |
| `dependency-review` | Blocks PRs only for **high/critical** CVEs in **runtime**-scoped deps; dev-only packages are exempt by scope | `.github/workflows/dependency-review.yml:27-29` |
| CodeQL | Runs on `develop` **and `main`** pushes/PRs, plus weekly | `.github/workflows/codeql.yml:33-39`; the docblock at `:14-32` records why `main` is listed even though the `develop` → `main` flow already scans the code — a hotfix pushed straight to `main` bypassed analysis entirely |

---

## Load-bearing invariants

| Invariant | Enforced by (path:line) | What breaks if violated | How it fails |
|---|---|---|---|
| **Graph node ↔ content bijection.** Every `graphNodes` id maps to real content, and every work/project slug is reachable from the graph | `src/lib/game-model.test.ts:22-40`, `:42-53`, `:55-58`; data at `src/lib/graph-data.ts:18-41` and `src/lib/game-model.ts:28-50` | Adding a content MDX file without a graph node — or a node without content — orphans one side. `resolveNode()` returns `null` for an unmapped id and `questNodes` drops it silently at runtime (`src/lib/game-model.ts:62-71`, `:96-109`) | **Loud** at build (test failure); would be **silent** at runtime without the test |
| **The three intentional node-id ≠ slug mappings** `aava`→`aava-code`, `grpc`→`grpc-microservices`, `nhl`→`not-humans-lab` | `src/lib/game-model.ts:31`, `:42`, `:45` — each carries the literal comment `// node id != slug`. Verified verbatim. All 13 other mappings are identity | "Fixing" one of these to match its slug orphans the node and fails the bijection test. Node ids also flow into `build-graph.tsx:44` (`questNodes.find(n => n.id === selectedId)`) | **Loud** at build |
| **`register` is the canonical contribution attribution.** Never fabricate ownership | Required by Zod at `velite.config.ts:42`; flows verbatim into the corpus as `Contribution: ${w.register}` (`src/lib/corpus.ts:17`) and is prefixed into the résumé summary as `` `${w.register}. ${w.summary}` `` (`src/lib/resume-json.ts:30`) — both verified verbatim. Also surfaced at `src/app/work/page.tsx:53`, `src/app/work/[slug]/page.tsx:75`, `src/app/work/[slug]/opengraph-image.tsx:21`. Policy at `CLAUDE.md:365`, `ARCHITECTURE.md:95` | Making it optional silently drops attribution from the LLM context, the machine-readable résumé, the OG cards and the case-study pages at once. The 5 shipped values are `Co-built · production-hardened`, `Co-built · architected the backend`, `Co-built · production-ready` (×2), `Co-built` | **Loud** if removed from the schema; **silent** if the string is weakened |
| **`emittedAny` streaming-fallback rule.** Fallback to the next model is possible only while ZERO bytes have been sent | `src/lib/llm.ts:433` (verified verbatim); rationale `:149-157`; test `src/lib/llm.test.ts:253-309`. The same flag also gates trace-frame emission (`:405-412`) and the one-shot `THINKING_SENTINEL` (`:330-332`) | Widening the condition lets a mid-stream error retry, duplicating already-delivered text and attributing bytes to the wrong model in the trace frame | **Loud** at build (test); **silent** in production if the test were removed |
| **Card-token slug allowlist is fail-closed in three ways**: locked charset, resolution against the build-time Velite allowlist, and outright DROP of unresolved tokens | `src/components/chat/parse-cards.ts:33` (verified verbatim), `:54-68`; allowlist `src/lib/content.ts:43-44`; test `src/components/chat/parse-cards.test.ts:55-75` | A hostile or hallucinating model could conjure a card, an href, or a path for content that does not exist. `cmd:view` is additionally restricted to the exported `VIEWS` tuple (`src/components/view-context.tsx:26`, exported `:198`) | **Loud** at build (test) |
| **`ChatCard` renders only Velite fields.** The model chooses *which* card, never its contents | `src/components/chat/chat-card.tsx:12-77` — reads only `p.url/repo/name/tagline/tech/commits/group` and `w.url/register/name/summary/metrics` | Interpolating model text into a card reintroduces the fabrication surface the token allowlist exists to close | **Silent** — no test asserts the field set |
| **Streamed markdown is XSS-safe by construction** | `src/components/chat/markdown-message.tsx:88-93` — react-markdown vdom + `skipHtml` + default `urlTransform` (not overridden) + `rehypeSanitize`; documented `:10-16` | Removing `skipHtml` or overriding `urlTransform` re-enables raw model HTML and `javascript:` hrefs. `rehypeSanitize` is documented as defense-in-depth, not the primary guard | **Silent** — `markdown-message.test.ts:9-11` explicitly disclaims XSS coverage |
| **Anthropic usage fields are snake_case** | `src/lib/llm.ts:162-169`; pinned by `src/lib/llm.test.ts:244-249`; read by `src/app/admin/telemetry/page.tsx:59-66` | A camelCase SDK would zero out every token/cache tile on the dashboard with no error anywhere | **Loud** at build (dedicated test); would be **silent** at runtime |
| **Trace frame is additive-only** | `src/lib/llm.ts:220-222` — `splitTrace` in `use-chat.ts` `JSON.parse`s and spreads, so unknown keys survive | *Removing* a key is the breaking change, not adding one. Dropping `model`/`fellBack` silently kills the model badge (`src/components/chat/chat-messages.tsx:557-559`) | **Silent** |
| **`PLACEHOLDER_SENTINEL` is the shipping switch for the glass-box demo** | `src/lib/agent-trace.ts:22` (`"[DRAFT — owner to approve]"`), gate computed `:120-122` (verified verbatim), consumed `src/components/game/glass-box-demo.tsx:40` (`if (!traceApproved) return null;`) | Removing or renaming the sentinel string **immediately ships** the un-reviewed demo prose. It is currently present — **16 occurrences on 15 lines** (`grep -o` count = 16, `grep -c` = 15, re-run on the current tree): declaration `:22`, header comment `:11`, 12 interpolations at `:59-60`, `:66-67`, `:73-74`, `:86-87`, `:93-94`, `:100-101`, and 2 in the predicate at `:121` — so `traceApproved === false` and the component renders nothing | **Silent** — the test only checks flag/prose agreement (`src/lib/agent-trace.test.ts:56`) |
| **Every `agent-trace` ref slug must resolve** | `src/lib/agent-trace.test.ts:20-28`; `linkForSlug` at `src/lib/agent-trace.ts`; render guard `src/components/game/glass-box-demo.tsx:114-124` | A ref to nonexistent content would render a dead link claiming a system exists. Referenced slugs: `aava-code`, `mindforge`, `pensieve` | **Loud** at build |
| **Server + first-client view snapshot is always `classic`** | `src/components/view-context.tsx:65` (verified verbatim: `getServerSnapshot = (): View => DEFAULT_VIEW`), `:61-63` comment, `:49` module init; test `src/components/view-context.test.ts:30-34` | Making it dynamic causes a hydration mismatch and serves a non-Classic view to crawlers and no-JS visitors. The deep link is applied only post-hydration by `ViewQuerySync` (`view-context.tsx:169`), isolated behind its own `<Suspense>` (`:181-183`) so `useSearchParams` does not client-render the whole provider tree | **Loud** at build (test) |
| **`flushSync(emit)` must be INSIDE the `startViewTransition` callback** | `src/components/view-context.tsx:115`, rationale `:70-77` | `useSyncExternalStore` emits are batched; without `flushSync` the transition's "after" DOM snapshot is still the old view, so nothing animates | **Silent** — the transition just doesn't happen |
| **Two named view-transition groups** — `viewTransitionName: "view-body"` and `"site-header"` | `src/components/view-router.tsx:56`; `src/components/site-nav.tsx:40`; consumed by `src/app/globals.css:270-283` and `:284-289` | Removing either silently kills the slide CSS (or lets the sticky nav fade with the body). Direction comes from `document.documentElement.dataset.viewDir` stamped at `view-context.tsx:133-136` | **Silent** |
| **Classic is hidden, never unmounted; the other views ARE unmounted** | `src/components/view-router.tsx:58` (`hidden`), `:25-50` (`next/dynamic`, `ssr:false`), rationale `:9-24` | Hiding the gamified view instead of unmounting it leaks a live WebGL context on low-end mobile; unmounting Classic loses scroll position | **Silent** (GPU memory growth) |
| **Corpus required sections** | `src/lib/corpus.ts:60-76` fixed section order; test `src/lib/corpus.test.ts:13-18`. Conditional sections collapse to `""` when empty: personal `:39`, testimonials `:50`, notes `:56` | Losing `## Production Work` or `## Skills` silently degrades every chatbot answer. An emitted-but-empty `## Personal` section is what the IFF assertion prevents | **Loud** at build |
| **Prompt can only advertise resolvable card slugs** | `src/app/api/chat/route.ts:66-67` computes `PROJECT_SLUGS`/`WORK_SLUGS` from Velite at module load and interpolates them at `:128-129` | Hardcoding a slug list into the prompt lets the model emit a token the client will drop, producing an answer that references an invisible card | **Silent** |
| **`.velite/` is gitignored, so build order is `velite → vitest → next build`** | `.gitignore:45`; `src/lib/content.ts:14`; `package.json:11` (all verified verbatim); CI mirror `.github/workflows/ci.yml:43-44` | Reordering the chain, or splitting it into independent commands, removes the "failing test blocks deployment" property entirely | **Loud** (resolution error) for ordering; **silent** for the `&&` → `;` change |
| **`register`-adjacent: `metrics[].value` is a string** | `velite.config.ts:44` (`s.object({ value: s.string(), label: s.string() })`) | It is a string so `"1.5h → 15m"` and `"65% → 85%"` are legal. Tightening to `s.number()` fails the Velite build for 2 of the 5 Work files | **Loud** |
| **CRON_SECRET guard is fail-CLOSED and identical in all five routes** | `src/app/api/cron/eval/route.ts:98-102`, `health-check/route.ts:146-150`, `github-sync/route.ts:18-22`, `seo-audit/route.ts:16-20`, `content-audit/route.ts:19-23` | An unset `CRON_SECRET` 401s every cron rather than opening it. The compare is a plain non-constant-time `!==` against `` `Bearer ${secret}` `` with a lower-case header name — no scheme-case tolerance, no trimming | **Loud** (401s in the cron log) |
| **`/admin/*` is gated entirely upstream in the proxy** | `src/proxy.ts:22-24` (`matcher: ["/admin/:path*"]`); the page itself does not check auth and says so at `src/app/admin/telemetry/page.tsx:425-426` | Widening or narrowing the matcher silently exposes or locks the dashboard. The Edge compare is a plain `!==` of two SHA-256 hex digests, documented as a first filter and **not** constant-time (`src/proxy.ts:12-20`, `:67-76`) | **Silent** (an unprotected page renders normally) |
| **One shared rate-limit budget** — 8 req / 60 s sliding window, prefix `anvilry:chat` | `src/lib/rate-limit.ts:22-25`; consumed by `/api/chat`, `/api/tts`, `/api/tts-google`, `/api/transcribe`, `/api/error` | The five routes are **not** independently budgeted. `/api/visit` has its own limiter — `slidingWindow(1, "30 m")`, prefix `anvilry:visit` (`src/app/api/visit/route.ts:38-45`) | **Silent** |
| **Rate limiting fails OPEN on both paths** | `src/lib/rate-limit.ts:73` (unconfigured → `{ ok: true }`), `:79-82` (any Upstash throw → warn + allow); rationale `:66-68` | Upstash going down degrades to "no limit", not "no chat" — deliberate. The only signal is a production-only module-load warning (`:41-47`) | **Silent** by design |
| **IP derivation is a security control:** `x-vercel-forwarded-for` first segment → `x-forwarded-for` **LAST** segment → `x-real-ip` → `"anonymous"` | `src/lib/rate-limit.ts:53-59`, rationale `:49-52`; duplicated verbatim at `src/lib/telemetry/with-trace.ts:63-73` (deliberately, `:56-62`); pinned by `src/lib/telemetry/with-trace.test.ts:220-230` | Taking the *first* XFF segment allows rate-limit bypass via a rotating spoofed header. **Fixed on this branch:** `/api/visit` used to take the leftmost segment — the third copy of `clientIp` — and now takes the **last** like the other two (`src/app/api/visit/route.ts:25`, rationale `:26-30`). The duplication is now guarded rather than trusted: `src/lib/client-ip-consistency.test.ts` enumerates the copies (`:26`), fails if an unlisted copy appears anywhere under `src/` (`:101`), and pins accessor order (`:131`), LAST-segment choice (`:140`) and the fallback chain (`:154`) for each — against comment-stripped source, because an earlier draft of that test matched the explanatory comment and therefore could not fail (`:20-23`) | **Loud** at build for all three copies (previously silent for `rate-limit.ts` and `/api/visit`) |
| **`emit()` does no redaction — callers must redact first** | `src/lib/telemetry/emit.ts:30-35`; callers `src/app/api/chat/route.ts:293`, `src/app/api/error/route.ts:145-166`, and the four `server.error` paths | "Garbage in → garbage stored." A new emit site that skips `redact()` writes PII to the trace log and to Redis | **Silent** except for `/api/error`, which is pinned by `src/app/api/error/route.test.ts:218-255` |
| **`DEDUPE_FLAG = "__anvilry_error_recently__"` is a shared string across three files** | `src/app/error.tsx:39`, `src/app/global-error.tsx:33`, `src/instrumentation-client.ts` (100 ms window, `:26-39`, `:65-72`) | Renaming it in one place double-beacons every error to `/api/error`. It is a timestamp rather than a boolean specifically to avoid a `setTimeout` cleanup that would re-create the race | **Silent** (duplicate rows in telemetry) |
| **`/api/error`'s `source` enum is the declared source of truth** | `src/app/api/error/route.ts:83`; must match `ErrorBeaconPayload` at `src/lib/telemetry/beacon.ts:42` | A legitimate beacon with an unlisted `source` 400s and the error is lost silently | **Silent** |
| **`disableSse: true` on the MCP transport** | `src/app/api/mcp/[transport]/route.ts:126`, rationale `:120-125` | Without it, a GET to `/api/mcp/sse` (the `[transport]` segment matches `"sse"`) falls into mcp-handler's Redis init and throws `redisUrl is required` → unhandled 500, because this project uses Upstash REST, not `REDIS_URL`/`KV_URL` | **Loud in production** (500), silent locally |
| **MCP not-found is keyed on the literal `notFound` property** | `src/app/api/mcp/[transport]/route.ts:13`; produced by `src/lib/mcp-tools.ts:42-48` | Renaming that key turns every not-found into a **success** response, so the agent receives a fabricated-looking empty answer instead of the list of valid options | **Silent** |
| **`ROLE_TO_LABEL` hardcodes exact `resumeVariants[].label` strings** | `src/lib/mcp-tools.ts:20-26` vs `src/lib/profile.ts:76-78` (the single `"Sairam Resume"` label) | A label rename breaks `get_resume_variant`. Partially caught: `src/lib/mcp-tools.test.ts:65` asserts every role resolves to a variant whose PDF exists on disk | **Loud** at build (via the PDF-existence assertion) |
| **`three` etc. must be imported through the `src/lib/r3f.ts` barrel** | `src/lib/r3f.ts:5-13`; `next.config.ts:149` states outright "The `src/lib/r3f.ts` barrel stays — it is load-bearing for the single-copy outcome" | Bypassing it re-splits three.js into duplicate chunks. Recorded measurement — and it is a **Turbopack** measurement, i.e. of what actually ships (see the `next build` row under [Environment & platform gotchas](#environment--platform-gotchas)): 16.2.9 = 2036 KB / 5 chunks (two 876 KB copies) → 16.3.0 = 1160 KB / 4 chunks (`next.config.ts:133-144`), with `WebGLRenderer` in exactly ONE chunk (`next.config.ts:140-142`). Re-confirmed on the `.next/` tree present on the development machine: one chunk, **897,249 B**. `src/components/hero-graph/scene-physics.tsx:4-6` is the one current violator | **Split.** Duplication stays **Silent** — twin *lazy* chunks change no route's first-load bytes, so the budget gate cannot see them. Dragging three.js onto the first-load path is now **Loud in CI** (`scripts/bundle-budget.mjs:124-127`) |
| **`three`/`@react-three/*` must stay OUT of `optimizePackageImports`** | `next.config.ts:150` (`["lucide-react", "motion"]`), rationale `:126-131`, `:146-148` | Investigation C-3 disproved the flag for the R3F twin-chunk; re-adding it re-introduces a known-ineffective optimisation against the live barrel | **Silent** |
| **The bundle budget gate must stay un-softenable: no `continue-on-error`, and an *unmeasurable* bundle must stay RED** | `scripts/bundle-budget.mjs`, wired at `.github/workflows/ci.yml:115-116` (rationale `:104-109`). Two assertions: **(a)** every route's `firstLoadUncompressedJsBytes` < **1,285,000 B** (`:45`) — largest today is `/` at **1,220,794 B**, 64,206 B of headroom, and the on-disk sum of that route's `firstLoadChunkPaths` is the identical 1,220,794 B, so it is a measurement and not a model; **(b)** the `WebGLRenderer` marker (`:57`) appears in **0 of the 16** routes' first-load sets, i.e. three.js stays lazy (`:119-127`). A missing, malformed, short, key-renamed or on-disk-absent artifact **exits 1 by design** (`:25-26`, `:64-73`, `:75-86`, `:110-117`) | **Adding `continue-on-error: true` — or any `if-no-files-found: warn`-shaped softener — silently reverts this to its predecessor.** `.github/workflows/bundle-analysis.yml` carried exactly those two settings and was therefore green with **zero artifacts for its entire life**: 222 runs, 211 green / 11 failed, and no artifact across the 25 most recent. (The counter is now FINAL: deleting the workflow file means no further runs can be recorded, so `scripts/bundle-budget.mjs:6` and `.github/workflows/ci.yml:110` state the same 222 rather than a live figure that rots.) Assertion (b) is the one a total-bytes guard cannot make: an eager `import * as THREE` in a shell component moves ~876 KB onto every route's critical path while total emitted bytes barely move (`scripts/bundle-budget.mjs:57-60`). Raising the ceiling is expected — do it in its own commit quoting measured before/after bytes, the discipline `next.config.ts:127-149` already uses (`scripts/bundle-budget.mjs:47-48`, `:133-134`) | **Loud in CI.** Not in `pnpm build`, so it never blocks a deploy — see § D of [Deploy blockers](#deploy-blockers). Whether a red `e2e` job blocks a *merge* is the open branch-protection question in [UNVERIFIED](#unverified) |
| **Exactly one live WebGL context** | `src/components/game/build-graph.tsx:50` mounts only when `isDesktop && !reduced && webglOk && !webglFailed && !talkOpen`; `talkOpen` ORs all three voice stores (`:38`). Hero slots use the same triple `isDesktop && !reduced && view === "classic"` (`src/components/hero-graph/index.tsx:35`, `src/components/hero-avatar/index.tsx:56`), the classic-view term documented at `hero-graph/index.tsx:22-24` | Dropping any term lets two canvases coexist; on low-end mobile a leaked context is a hard crash | **Silent** |
| **R3F context failure is async and boundaries cannot catch it** | `src/lib/use-media-query.ts:21-26`, `:28-47` (`useWebGLSupported`, memoized probe); `src/components/game/webgl-boundary.tsx` only catches sync throws | A boundary alone is insufficient — the Canvas must be skipped entirely. `build-graph.tsx:29` and `voice-orb.tsx:38` probe proactively; `hero-graph/index.tsx` and `hero-avatar/index.tsx` do **not** | **Silent** (blank area) |
| **`continuous = false` on browser SpeechRecognition** | `src/components/chat/use-speech-recognition.ts:162`; consequence documented `src/components/chat/use-voice-session.ts:26-31` | It is why the mic is already off during thinking+speaking, why self-hearing is impossible, and why barge-in is a UI interrupt rather than voice | **Silent** (self-hearing loop) |
| **`useVoiceSession` stores only `active`** — listening/thinking/speaking/paused are derived every render | `src/components/chat/use-voice-session.ts:81`, `:248-253` | Storing derived turn state reintroduces the class of desync bugs the derivation exists to prevent | **Silent** |
| **The `?view=voice` view is NOT a mutex participant** | `src/components/chat/voice-surface-mutex.ts:18-20`; enforced instead by `src/components/chat/header-orb-trigger.tsx:67-70` disabling the orb when `view === "voice"` | Registering it would make a view closable by an overlay; removing the orb guard stacks a second concurrent mic | **Silent** (two mics) |
| **`WakeWordController` is scoped to `ACTIVE_VIEWS = new Set(["chat"])`** | `src/components/chat/wake-word-controller.tsx:29`, rationale `:22-26`; pinned by `wake-word-controller.dom.test.tsx` | Arming it on the voice view stacks the modal's session over the view's live one | **Loud** at build (test) |
| **`terminal.tsx`'s `aria-label="Terminal command input"` is a selector contract** | `src/components/game/terminal/terminal.tsx:254`; queried verbatim by `src/components/game/terminal/terminal-overlay.tsx:39-41` | Renaming the label breaks initial focus in the fullscreen overlay | **Loud** at build (`terminal-overlay.dom.test.tsx`) |
| **`COMMAND_NAMES` must stay `!hidden`-filtered, in two independent places** | `src/components/game/terminal/commands.ts:525-527` (Tab completion) and `src/components/game/terminal/terminal.tsx:17-19` (fuzzy dropdown) | Dropping either filter broadcasts the 4 hidden eggs (`secret`, `personal`, `uses`, `now`) into help/autocomplete | **Loud** at build (`commands.test.ts:26-27`) |
| **The three project-group strings are duplicated in three files** | `velite.config.ts:4-8` (Zod enum), `src/lib/content.ts:30-34` (`projectGroups`), `src/lib/game-model.ts:177-181` (`projectGroupOrder`) | Renaming a group in one place fails the Velite build (if the enum) or silently drops a whole `/projects` section and a whole quest group (if only a consumer) | **Mixed** — loud from the enum, silent from the consumers |
| **`getVoiceById` must never 500 on an unknown id** | `src/lib/voice-catalog.ts:322-323` — callers MUST fall back to `getDefaultVoiceId()` | A stale localStorage `voiceId` would otherwise break voice on load. The catalog's three lookup Maps are built at module load (`:298`, `:300-305`, `:307-312`) — a voice added anywhere but `CURATED_VOICES`/`EXTENDED_VOICES` is un-lookupable and un-allowlisted | **Silent** |
| **`use cache` must be the first statement of `/projects`' function body** | `src/app/projects/page.tsx:38-39` (`"use cache"` then `cacheLife("hours")`), rationale `:31-36` | It is the only page with a cache directive; the built artifact confirms `initialRevalidateSeconds: 3600` / `initialExpireSeconds: 86400`. `/work` and `/notes` deliberately have none because their only input is the build-time Velite import (`src/app/work/page.tsx:9-13`, `src/app/notes/page.tsx:9-11`) — do not "restore consistency" | **Loud** (build error) if misplaced; **silent** if wrongly added elsewhere |
| **Open-redirect guard on article redirects** | `src/app/articles/[slug]/page.tsx:77-81` — the external URL must start with `https://` or `http://`, else `notFound()` | Relaxing it lets a `javascript:` or `data:` frontmatter value become a live redirect | **Silent** |

---

## Environment & platform gotchas

### AWS / Bedrock

| Gotcha | Cite | Detail |
|---|---|---|
| **`AWS_REGION` is corrupted on Vercel.** Use `BEDROCK_REGION` | `src/lib/llm.ts:84-87`; `.env.example:14-16`; `CLAUDE.md:333` | `AWS_REGION` is reserved on Vercel/Lambda and was observed in production as `"s-east-1"` — the leading `u` stripped. Resolution order is `BEDROCK_REGION \|\| AWS_REGION \|\| "us-east-1"` (`src/lib/llm.ts:87`), so `BEDROCK_REGION` shields it. **`.env.example:18` nonetheless sets `AWS_REGION=us-east-1` uncommented, directly under its own warning** |
| **Bedrock secrets are base64-or-raw, discriminated by a round-trip re-encode** | `src/lib/llm.ts:63-72` (verified verbatim: `if (Buffer.from(decoded,"utf-8").toString("base64") === value) return decoded;`) | A plain "decodes without throwing" test is too loose — many raw secrets are coincidentally valid base64. Raw `AKIA…` keys are not valid base64 *of themselves*, so they fall through unchanged. Empty/undefined → `""` (`:64`) |
| **Decoded creds must go through the double-async resolver** | `src/lib/llm.ts:119-123` — `providerChainResolver: async () => async () => ({...})` | The resolver returns a credential *provider*, which returns credentials. Without this the AWS default chain signs with still-base64 values |
| **Opus 4.6 on Bedrock REQUIRES the `-v1` suffix** | `src/lib/llm.ts:27-33` (verified verbatim comment: "Opus 4.6 REQUIRES the `-v1` suffix — the bare id 400s with 'model identifier is invalid'. Sonnet 4.6's bare id resolves fine.") | Chain is `us.anthropic.claude-sonnet-4-6` → `us.anthropic.claude-opus-4-6-v1` → `us.anthropic.claude-haiku-4-5-20251001-v1:0` (`src/lib/llm.ts:31-35`). The direct-API chain is **not** version-parallel: its secondary is `claude-opus-4-7` (`:38`) |
| **Provider toggle is a single exact-string check** | `src/lib/llm.ts:53` — `process.env.LLM_PROVIDER === "anthropic" ? "anthropic" : "bedrock"` | Anything else, including unset and a typo, is Bedrock |
| **AWS is called server-side only** | `next.config.ts:56-67` CSP rationale; import sites `src/app/api/tts/route.ts:1`, `src/app/api/transcribe/route.ts:1-5` | Which is why `connect-src` contains **no** AWS host. Adding a browser-side AWS call requires a CSP change |
| **`BEDROCK_PRICE` is a hardcoded table** | `src/app/api/chat/route.ts:24-46`, fallback `:49-50` | Unknown model ids silently fall back to Sonnet 4.6 pricing, yielding *wrong but non-zero* `cost_usd` |
| **`llm.ts` reads exactly 7 env vars and does NOT read `EXTENDED_THINKING`** | `src/lib/llm.ts:53`, `:79`, `:80`, `:81`, `:87`, `:97`; the route reads it at `src/app/api/chat/route.ts:263` | `EXTENDED_THINKING` is on **unless** the exact string `"false"` — an unset env means thinking is enabled |

### Build & content pipeline

| Gotcha | Cite | Detail |
|---|---|---|
| **Never pass `--clean` to Velite in dev** | `velite.config.ts:121-125` (`clean: false`); `package.json:9` (`"predev": "velite"`, no flag); `next.config.ts:12-16` (dev watcher started with `{ watch: true, clean: false }`) | `--clean` races the dev compiler into "Can't resolve './projects.json'". `next.config.ts:11` calls that compiler **webpack**; in Next 16 a bare `next dev` is **Turbopack** too — `next dev` routes through the same `parseBundlerArgs` default (`node_modules/next/dist/cli/next-dev.js:173`, `node_modules/next/dist/lib/bundler.js:142-144`) — so the race is real but the bundler name in that comment is stale. Do not treat the stale name as evidence the race is gone. Production purity comes from the explicit `--clean` in the `build`/`content` scripts (`package.json:11`, `:17`), not from the config. `next.config.ts:13-14` double-guards the watcher with `process.argv.includes("dev")` and a `VELITE_STARTED` sentinel |
| **`pnpm clean` deletes `.velite`** | `package.json:18` | It must always be followed by `pnpm content` before `test` or `tsc` |
| **`pnpm analyze` is a LOCAL tool, and its `--webpack` flag is load-bearing** | `package.json:12` (`velite --clean && ANALYZE=true next build --webpack`); enable gate `next.config.ts:6` (`enabled: process.env.ANALYZE === "true"`); wrapper `next.config.ts:5-7`, applied `:232` | Added on this branch as the *local* replacement for the deleted bundle-analysis workflow. Drop `--webpack` and the run is a Turbopack build, where `@next/bundle-analyzer` prints one warning and returns the config untouched (`node_modules/@next/bundle-analyzer/index.js:7-15`) — the same silent nothing the workflow shipped for its whole life. The package names the flag itself at `index.js:12`. Output is `.next/analyze/{client,edge,nodejs}.html` (`index.js:27-31`). **Deliberate consequence:** a `--webpack` build does **not** write `.next/diagnostics/route-bundle-stats.json`, so `node scripts/bundle-budget.mjs` correctly FAILS straight after `pnpm analyze` and names `--webpack` as the cause (`scripts/bundle-budget.mjs:73-77`). Re-run `pnpm build` before the gate, and never put `analyze` in CI |
| **`pnpm search-index` does not exist** | `package.json:8-22` (**12** scripts since this branch added `analyze` at `:12`, none named `search-index`); the only Pagefind entry point is `Makefile:64-66` | **The doc side of this was FIXED on this branch:** `CLAUDE.md` used to print a bare `pnpm search-index` in its command list; `CLAUDE.md:38-39` now carries a `# NOTE:` line saying outright that this is a Makefile target only and that no such pnpm script exists, immediately above `make search-index`. The nonexistent script is no longer documented anywhere. The *code* half of the gotcha is unchanged and still live: `public/pagefind/` is absent from the tree, so `/search` 404s on its assets (`src/app/search/page.tsx:20`, `:25`) until `make search-index` runs **after** `make build` |
| **`s.slug()` is unique per group, not globally** | `node_modules/velite/dist/index.js:5095` | `how-dns-works` legitimately exists as both a Note slug and an Article slug. The Article version is the "notes-only article" case handled at `src/app/articles/[slug]/page.tsx:43`, `:65-66` |
| **`clean: false` also means stale `.velite/` output can persist** | `velite.config.ts:125` | The inspected snapshot was dated 15 Aug while content files were 28 Jun. Run `pnpm content` when in doubt |

### Testing & CI

| Gotcha | Cite | Detail |
|---|---|---|
| **`NODE_ENV` is forced to `"test"` in Vitest** | `vitest.config.ts:26`, rationale `:19-25` | Vitest only defaults it when unset; the Vercel build shell sets `production`, React loads its prod bundle without `act`, and every DOM test dies with "React.act is not a function" — failing the deploy. The pin is scoped to the Vitest worker; `next build` still runs in production mode |
| **Tests run twice per CI run** | `.github/workflows/ci.yml:52-53` (`pnpm test`) and `:101-102` (`pnpm build`, which re-runs `vitest run`) | Expected, not a bug |
| **Playwright needs a production build first** | `playwright.config.ts:33-40` — `webServer.command = "pnpm start"`, `reuseExistingServer: !CI` | Hence `pnpm build` precedes `pnpm e2e` in CI (`.github/workflows/ci.yml:101-102` then `:113-114`, with the bundle budget gate wedged between them at `:110-111`). The `webServer` block was added because a stale server on :3000 made Playwright silently test an older build (`playwright.config.ts:23-32`) |
| **Both Vitest include globs carry a dead `tests/**` arm** | `vitest.config.ts:33`, `:42` | There is no `tests/` directory in the repo |
| **CI and production run different Node majors** | `.github/workflows/ci.yml:27` (Node 22, pnpm 10); gitignored `.vercel/project.json` records `"nodeVersion": "24.x"` | **Local is now pinned, production still is not.** v3.5.0 added `.nvmrc` (`22`) and `engines.node` (`package.json:5-7`, `">=22 <23"`) to match the CI pin — a contributor on Node 26 got 9 failing tests and a red `pnpm build` because Node's native `localStorage` collides with vitest's happy-dom injection (`CHANGELOG.md:178-181`). Vercel's `24.x` is outside that range and is set in the dashboard, not the repo, so `engines` does not constrain it. Still no `packageManager` field and no `.npmrc` |

### CSP & headers

| Gotcha | Cite | Detail |
|---|---|---|
| **`'unsafe-eval'` is required in BOTH dev and prod because MDX is evaluated at runtime** | `next.config.ts:43-51`; cause `src/components/mdx-content.tsx:14-17` (`const fn = new Function(code); return fn({ ...runtime }).default;`) | Velite emits a serialized `code` string that `MDXContent` deserialises **client-side on every project/work/note page**. Removing `'unsafe-eval'` crashes those pages with a React render-error boundary. `next.config.ts:43-50` records that an earlier comment claiming "Velite pre-compiles MDX at build time" was incorrect |
| **`MDXContent`'s `code` is a trust boundary** | `src/components/mdx-content.tsx:8-13` | It must only ever be Velite output from the build-time `content/` directory. Any runtime/request-derived string there is arbitrary code execution |
| **The CSP is ENFORCED, not Report-Only** | header key at `next.config.ts:94` (`Content-Security-Policy`); accurate note at `:84-93`; the stale claim is at `:19-20` | The block comment still says "shipped as Report-Only first … to be promoted" |
| **The `/resume` override is a literal string replace** | `next.config.ts:204-207` replaces `frame-ancestors 'none'` → `'self'`; base directive at `:41`; applied at `:216-217` | Reordering or requoting `next.config.ts:41` silently makes the override a no-op and the résumé PDF iframe stops rendering (`src/app/resume/page.tsx:119-127`) |
| **Three speech hosts are allowlisted in `connect-src` for specific browsers** | `next.config.ts:56-67`, restated `:26-28` | `wss://speech.googleapis.com` — Chrome/Chromium Edge; browser `SpeechRecognition` opens a cross-origin WebSocket subject to `connect-src` since Chrome 63. Without it **every** recognition attempt fails with `onerror.error === "network"` and voice is permanently broken in production. `wss://speech.platform.bing.com` — Edge on Windows. `https://www.gstatic.com` — Chrome's online `SpeechSynthesis` voices (omitting it degrades quality, non-breaking). Recorded blind spot at `:60-61`: the Playwright zero-violation sweep never exercised live mic input |
| **HSTS is deliberately absent** | `next.config.ts:73-74` | Stated as already set by Vercel's platform default; a duplicate here would be weaker |
| **`worker-src 'self' blob:` was added for a worker that never existed** | `next.config.ts:68`; absence verified in section 07 | The directive was written for `@react-three/offscreen`, which was declared but imported nowhere and was **removed in v3.5.0** (`CHANGELOG.md:197-198`) — there is still no R3F worker or `OffscreenCanvas` anywhere in `src/`. **Do not delete the directive on that basis.** The one real worker in the tree is pdf.js's: `GlobalWorkerOptions.workerSrc` is set to a bundler-resolved same-origin URL (`src/components/chat/file-picker-button.tsx:79-82`), reached only when `NEXT_PUBLIC_PDF_ATTACHMENTS === "true"` (`:7`) and a PDF is selected. The `'self'` arm covers that; the `blob:` arm has no located consumer |

### Flags & env semantics

| Gotcha | Cite | Detail |
|---|---|---|
| **`NEXT_PUBLIC_*` is inlined at build time** | `src/lib/writing-flags.ts:16` | Toggling any of them requires a redeploy — including the orb flags (`src/components/chat/header-orb-trigger.tsx:32`) |
| **`ARTICLES_ENABLED` is the only opt-OUT flag** | `src/lib/writing-flags.ts:19-20` (`!== "false"`, default **true**) | All 8 other booleans in that file are opt-in `=== "true"` defaulting false. Mixing the conventions flips a section's default |
| **`NEXT_PUBLIC_ENABLED_VIEWS`: unset ≠ empty string** | `src/lib/enabled-views.ts:23`, `:27-34` — the distinguishing check is `raw === undefined \|\| raw === null` | Unset → all optional views on. Set to `""` → all optional views off. Unknown entries are silently dropped by the `ALL_OPTIONAL.includes(v)` filter (`:33`), so a typo disables that view rather than erroring. `classic` and `resume` are unconditionally true (`:20`, `:38`) |
| **`FLAG_DRIVER` is captured once at module load** | `src/lib/flags.ts:12-13` | Changing it without a process restart has no effect. `FLAG_DRIVER=vercel` uses the Flags SDK declaration (`:17-29`); anything else falls to build-time `process.env.NEXT_PUBLIC_DISCOVERY_BADGES === "true"` (`:58`). Only **one** flag was migrated |
| **The Flags manifest endpoint declares exactly one flag** | `src/app/.well-known/vercel/flags/route.ts:11-22` | Key must match `flag({ key })` at `src/lib/flags.ts:18` and the Vercel dashboard id, including a hardcoded `origin` URL |
| **`/stats` and `/search` are NOT route-gated** | `STATS_ENABLED`/`SEARCH_ENABLED` consumed only by `src/app/sitemap.ts:80-88` and `src/components/site-nav.tsx:22-23` | Both routes render and prerender with the flags off — merely unlinked and un-sitemapped. Contrast `/articles` (gated in `src/app/articles/layout.tsx:12`) and `/notes` (gated in `src/app/notes/page.tsx:21`) |
| **`TELEMETRY_ENABLED` only affects `/api/error`** | `src/app/api/error/route.ts:89-95` — only the exact string `"false"` disables it | `.env.example:113` and `docs/configuration.md:97` say it "disables all event emission"; `/api/chat`, `/api/tts` etc. never check it |
| **11 author-set env vars are read in `src/` but absent from `.env.example`** | `GITHUB_TOKEN` (`src/lib/github.ts:90`), `EXTENDED_THINKING` (`src/app/api/chat/route.ts:263`), `FLAGS` (`src/instrumentation.ts:49`), and `NEXT_PUBLIC_{RESUME_VARIANTS, HERO_MODE, AVATAR_POSITION, GRAPH_PHYSICS, MULTIMODAL_ATTACHMENTS, PDF_ATTACHMENTS, EXTENDED_THINKING, INKFORGE_ARTICLES_ENABLED}` — 3 + 8 = 11, matching the 10 rows flagged "Undocumented in `.env.example`" plus the `GITHUB_TOKEN` row (worded "**Not documented in `.env.example` at all.**") in [11 § Environment variables](./11-config-build-ci-infra.md#environment-variables) | Reproduce by diffing `grep -rhoE 'process\.env\.[A-Z0-9_]+' src \| sort -u` against `.env.example`. **Exclusion rule:** the diff also lists five runtime-injected platform vars (`NEXT_RUNTIME`, `NODE_ENV`, `VERCEL_ENV`, `VERCEL_GIT_COMMIT_SHA`, `VERCEL_URL`) and the config-written `NEXT_PUBLIC_BUILD_YEAR` (`next.config.ts:107`); none is author-set, so none belongs in `.env.example` and all six are excluded from the count. `NEXT_PUBLIC_ENABLE_ANVIL_ORB` is **not** absent — it is documented as a legacy alias at `.env.example:57`. `make env-check` / `make flags-show` read the **process** environment, not `.env.local` (`Makefile:288-314`, `:154-180`), so they under-report locally |
| **`VERCEL_ENV`, not `NODE_ENV`, discriminates production** | `src/instrumentation.ts:86-92` | Vercel preview deploys also run `NODE_ENV=production`, which would pollute `anvilry:corpus:built_at` |
| **Grep handles are load-bearing and distinct** | `[config]` `src/instrumentation.ts:84`; `[trace]` `src/lib/telemetry/emit.ts:49`; `[vitals]` `src/instrumentation-client.ts:52`; `[flags]` `src/lib/flags.ts:45` | `make logs-flags` / `make logs-llm` grep on these (`Makefile:127-128`). The `[config]` snapshot logs presence only, never a secret value (`src/instrumentation.ts:12-13`) |

### The hardcoded base URL

`CLAUDE.md` used to say the base URL lived in **exactly four** files. **That was a live defect and it is
now FIXED:** `CLAUDE.md:335` says the host is hardcoded in **19 files / 25 occurrences**, "not four", and
`CLAUDE.md:338` gives the grep to run instead of trusting any list; `DEPLOY.md:146-158` was corrected the
same way. The 19/25 figure counts `src/lib/mcp-tools.test.ts:69`; this table deliberately excludes the test
file, so its count is **18 files / 24 occurrences** — the same set. Re-verify with
`grep -rn 'anvilry\.vercel\.app' src Makefile | grep -v '\.test\.'` — that command is the source of
truth for this table, and its output is what the counts below are taken from:

| File | Cite |
|---|---|
| `src/app/layout.tsx` | `:26` (`metadataBase`) |
| `src/app/sitemap.ts` | `:5` |
| `src/app/robots.ts` | `:6` |
| `src/components/json-ld.tsx` | `:29`, `:101`, `:125`, `:131`, `:160`, `:168` (`BASE_URL`), `:214` — **7** occurrences in one file; `CLAUDE.md:343` now records the same 7 (it used to name this file as one of only four, with no count). `:214` is inside FAQ answer *prose*, so a mechanical find-and-replace on the constant misses it |
| `src/app/mcp/page.tsx` | `:6` (`ENDPOINT`) |
| `src/app/articles/[slug]/page.tsx` | `:12` (`const BASE`) |
| `src/app/notes/[slug]/page.tsx` | `:13` |
| `src/app/projects/[slug]/page.tsx` | `:12` |
| `src/app/work/[slug]/page.tsx` | `:11` |
| `src/app/feed.xml/route.ts` | `:6` |
| `src/lib/llms-txt.ts` | `:5` |
| `src/lib/resume-json.ts` | `:4` |
| `src/lib/mcp-tools.ts` | `:14` |
| `src/app/opengraph-image.tsx` | `:37` — rendered footer **display text** (`<span>anvilry.vercel.app</span>`), not a URL constant |
| `src/app/articles/[slug]/opengraph-image.tsx` | `:62` — display text |
| `src/app/work/[slug]/opengraph-image.tsx` | `:55` — display text |
| `src/app/notes/[slug]/opengraph-image.tsx` | `:57` — display text |
| `Makefile` | `:19` (`PROD_URL`) |

Two more sites sit outside the `src/` + `Makefile` runtime scope above and are easy to miss:

| File | Cite | Why it matters |
|---|---|---|
| `src/lib/mcp-tools.test.ts` | `:69` | Strips the literal `https://anvilry.vercel.app` prefix to assert tool URLs. A domain change **fails this test**, and tests gate `pnpm build` — so the deploy breaks unless it is updated in the same commit |
| `next.config.ts` | `:195` | Comment only, no live URL — no code change needed, but the prose goes stale |

The four `opengraph-image.tsx` files render the domain as visible text in every generated OG card. Miss
them and the social preview images keep advertising the old domain after the migration is otherwise complete.

### Dependencies & platform pins

| Gotcha | Cite | Detail |
|---|---|---|
| **All pnpm settings live in `pnpm-workspace.yaml` — putting them back in `package.json`'s `pnpm` field silently disarms them** | `pnpm-workspace.yaml:18-28` (the ten `overrides`), `:44-45` (`onlyBuiltDependencies`), `:48-50` (`ignoredBuiltDependencies`), `:55-58` (`allowBuilds`), rationale in-file at `:1-12`; `package.json` no longer has a `pnpm` field at all | Moved in v3.5.0 (`CHANGELOG.md:184-194`). **pnpm 11 does not read the `package.json` `pnpm` field** — it prints one warning line and skips it. While the ten security `overrides` from v3.4.2 lived there, a single `pnpm install` on pnpm 11 would have regenerated `pnpm-lock.yaml` **without** the `overrides:` block and reverted that security release, with nothing failing: CI pins pnpm 10 (`.github/workflows/ci.yml:22`) and the lockfile already encoded the pins, so nothing was broken *yet*. Both pnpm 10 and 11 read the new location. Verified by regenerating the lockfile under each (byte-identical both times) and confirming all ten pins in the resolved graph (`CHANGELOG.md:190-194`) |
| **`three` has an effective ceiling of `<0.186.0`** | `package.json:53` declares `^0.185.1`; `postprocessing@6.39.4` peer is `three: >= 0.168.0 < 0.186.0` (`pnpm-lock.yaml:4003`) | One minor of headroom. The `three-webgl` Dependabot group batches `three`, `@react-three/*`, `@types/three`, `postprocessing` so the constraint is reviewed as a unit |
| **`@types/three` is load-bearing, not vestigial** | `package.json:32`; `node_modules/three/package.json` has no `types` field and no `.d.ts` in `build/` | `import type * as THREE from "three"` (`src/components/hero-avatar/rig.ts:4`) and `export * as THREE from "three"` (`src/lib/r3f.ts:21`) only typecheck via it. It is also the **sole** remaining consumer of `@dimforge/rapier3d-compat`, which is why removing `@react-three/rapier` in v3.5.0 downgraded that transitive from 0.19.2 to the 0.12.0 `@types/three` asks for (`pnpm-lock.yaml:6711`; `CHANGELOG.md:199-201`) — correct, not a regression |
| **`@modelcontextprotocol/sdk` is exact-pinned to `1.26.0`** | `package.json:27`; `mcp-handler@1.1.0`'s peer range is that single literal version (`pnpm-lock.yaml:3580`) | This pin is the direct cause of Dependabot's `security_update_not_possible`, which is why all 10 `overrides` exist (`pnpm-workspace.yaml:18-28`, mirrored `pnpm-lock.yaml:7-17`) |
| **`brace-expansion` needs TWO version-scoped overrides** | `pnpm-workspace.yaml:25-26` (`brace-expansion@1` → `^1.1.16`, `brace-expansion@>=3` → `^5.0.7`) | A blanket pin would force `minimatch@3` onto 5.x and break the eslint chain (`CHANGELOG.md:223-225`) |
| **`@react-three/postprocessing` must stay off 3.0.5** | `package.json:32` (exact `3.0.4`) paired with a Dependabot `ignore` scoped to `["3.0.5"]` (`.github/dependabot.yml`) | Types-only regression: `Omit<Partial<ConstructorParameters<...>[0]>,'offset'>` collapses to `{}` because the ctor param is optional. Runtime is fine — only `tsc` catches it. Loosening one half without the other lets it back in |
| **Dependabot reads `dependabot.yml` from the default branch only** | `CHANGELOG.md:252-253` | The `typescript`/`eslint` semver-major ignores were inert while they lived on `develop` |
| **`onlyBuiltDependencies: ["esbuild"]`** | `pnpm-workspace.yaml:60-61`; `esbuild@0.25.12` is a direct dep of `velite@0.4.0` (`pnpm-lock.yaml:9905-9908`) | pnpm 10 refuses install scripts unless allowlisted; content generation fails without it. The inverse list is `pnpm-workspace.yaml:64-66` (`ignoredBuiltDependencies: [sharp, unrs-resolver]`) — both lists moved out of `package.json` in v3.5.0 with the `overrides` |
| **The build-script allowlist is spelled TWICE — one spelling per pnpm major, and they must stay in sync** | `pnpm-workspace.yaml:60-66` (pnpm 10 lists) and `:71-74` (`allowBuilds`, pnpm 11 booleans) | pnpm 11 reads the old keys but `allowBuilds` wins, and when it is absent pnpm 11 **writes it into this tracked file** seeded with the string `set this to true or false` — not a boolean, so everything counts as denied and install exits 1 (`[ERR_PNPM_IGNORED_BUILDS]`). Measured: exit 1 on pnpm 11.17.0 from a clean clone, exit 0 on pnpm 10.34.5. **CI pins pnpm 10, so no green run could ever surface this** — it reached only contributors on the current default pnpm. Deleting either spelling re-breaks one major |
| **`web-vitals` is a devDependency that ships to the browser** | `package.json:75` (`^6.1.1`); dynamically imported from client-bundled `src/instrumentation-client.ts:50` | It resolves because Vercel installs dev deps at build time, but it is not a production dependency by declaration |
| **`zod` is v3 (`^3.25.76`) and `velite.config.ts` does not use it** | `package.json:55`; `velite.config.ts:1` imports Velite's own `s` builder | `src/lib/mcp-tools.ts:29-36` exports **raw-shape** schemas because that is what `mcp-handler`'s `registerTool` takes |
| **`next build` is TURBOPACK, not webpack.** This row previously asserted the exact opposite, sourced to the deleted `bundle-analysis.yml`'s own comment. **That was flatly false**, and it is the single most load-bearing correction on this branch | `node_modules/next/dist/lib/bundler.js:142-144` — the comment reads verbatim "The default is turbopack when nothing is configured", and the branch sets `process.env.TURBOPACK = 'auto'` and returns the Turbopack enum. A bare `next build` therefore prints `▲ Next.js 16.3.0 (Turbopack)`: `node_modules/next/dist/build/index.js:490-495` calls `logStartInfo` with `logBundler: true`, the suffix is assembled at `node_modules/next/dist/server/lib/app-info-log.js:81-82`, `▲` is `prefixes.ready` (`node_modules/next/dist/build/output/log.js:65`), and `bundlerName(0) === "Turbopack"` (`node_modules/next/dist/lib/bundler.js:83-92`) | Three consequences, in order of how badly each was misread. **(1)** `@next/bundle-analyzer` is webpack-only, so on a bare build it does **nothing**: under `process.env.TURBOPACK` it warns "The Next Bundle Analyzer is not compatible with Turbopack builds, no report will be generated" and returns `nextConfig` **unmodified** — no `webpack` key is ever added (`node_modules/@next/bundle-analyzer/index.js:7-15`; the hook it skips is at `:19-20`). It remains a devDependency (`package.json:58`) and still wraps the config (`next.config.ts:5-7`, applied `:232`, with `createRequire` at `:2`/`:4` because the package is CJS), but it is now a **local attribution tool only** and needs the explicit `--webpack` flag the package itself names at `index.js:12` — which is exactly what `pnpm analyze` passes (`package.json:12`). **(2)** The three.js single-876 KB-chunk invariant at `next.config.ts:127-149` **STANDS and must not be deleted** — it was always a *Turbopack* measurement (897,249 B, re-confirmed on the current tree), and so is `src/lib/r3f.ts`'s load-bearing role. Only the bundler attribution was wrong; correct the name, keep the invariant. **(3)** Turbopack is also the reason `.next/diagnostics/route-bundle-stats.json` exists at all: `writeRouteBundleStats` is gated on `bundler === Bundler.Turbopack` (`node_modules/next/dist/build/index.js:2843-2844`), and that artifact is what the bundle budget gate reads |
| **`turbopack: { root: __dirname }` pins the workspace root** | `next.config.ts:99` | Multiple lockfiles exist on the development machine. It governs `next build` as well as `next dev`, because both default to Turbopack (row above) — it is not a dev-only key |

---

## Silent-failure risks

Observations, not recommendations. Each is a place where a wrong edit produces no build error, no test
failure, and no runtime exception.

### Dangling references and missing assets

| Observation | Cite |
|---|---|
| Three Article `linkedNote` values point at notes that do not exist: `tombstone-v1-2-devto` → `tombstone-v1-2-release`, and both `trelix-v1-launch-devto`/`-substack` → `trelix-code-intelligence-engine`. `linkedNote` is a bare `s.string()` with **no** referential check, and no test references it. Consumers build `/notes/<linkedNote>` unconditionally, gated only on `NOTES_ENABLED` (default false), so the dead links become reachable the moment that flag flips | `velite.config.ts:105`; `src/components/article-card.tsx:18`; `src/components/article-group-card.tsx:21-22`; `src/app/articles/page.tsx:152-153`; `src/app/articles/[slug]/page.tsx:71-72`; `src/lib/llms-txt.ts:27-28`; flag `src/lib/writing-flags.ts:22-23` |
| **Fixed on this branch — the defect is gone.** `manifest.ts` used to declare PWA screenshots at `/static/screenshot-desktop.png` and `/static/screenshot-mobile.png` while `public/static/` was empty, so both 404'd in every environment. There is now **no `screenshots` key at all**; a NOTE records why it was dropped and how to restore it (`src/app/manifest.ts:19-24`). The manifest is also no longer un-guarded: `src/app/manifest.test.ts` asserts every declared icon `src` resolves to a real file (`:58`), asserts the two removed screenshot paths still have nothing behind them (`:44`), asserts the key is absent (`:66`), and — the part that prevents a repeat — asserts that *if* `screenshots` is ever declared again, every `src` must resolve (`:74`) | `src/app/manifest.ts:19-24`; guard `src/app/manifest.test.ts:66`, `:74` |
| **Fixed on this branch.** `llms-txt.ts` used to advertise the MCP endpoint as `${BASE}/api/mcp/sse` — a path the route deliberately 404s via `disableSse: true` — so `llms.txt` pointed AI agents at a dead transport. It now emits `${BASE}/api/mcp/mcp`, the live Streamable HTTP endpoint (`src/lib/llms-txt.ts:65`). `src/lib/llms-txt.test.ts` is the new guard: it asserts the advertised transport (`:14`), asserts `/api/mcp/sse` appears nowhere in the output (`:25`), and ties the assertion back to the route's `disableSse` setting so the two cannot drift apart again (`:29`) | `src/lib/llms-txt.ts:65`; route `src/app/api/mcp/[transport]/route.ts:126`; guard `src/lib/llms-txt.test.ts:25`, `:29` |
| `public/pagefind/` does not exist in the tree, so `/search` injects two 404ing assets at runtime; in dev the effect is a documented silent no-op | `src/app/search/page.tsx:6-8`, `:18-31`; `Makefile:64-66` |
| Résumé PDF filenames are hardcoded strings; renaming one produces a 404 with no type error (partially caught by the MCP PDF-existence assertion) | `src/lib/profile.ts:76-78`; guard `src/lib/mcp-tools.test.ts:65` |
| Five `public/*.svg` create-next-app scaffold files have no referrer anywhere in the repo | `public/{file,globe,next,vercel,window}.svg` (grep-verified, [11 § At a glance](./11-config-build-ci-infra.md#at-a-glance)) |

### Unwired / unreferenced code

| Observation | Cite |
|---|---|
| `src/lib/highlight-store.ts` exports `useHighlightedSlug` and `clearHighlight` but a repo-wide grep finds **zero** importers; only `highlightProject` is called. The highlight is written but never read. The docblock claims `project-card.tsx` subscribes — it does not import the module | `src/lib/highlight-store.ts:9-10`; caller `src/components/chat/chat-messages.tsx:15` |
| `src/lib/llm-sdk-mode.ts` has zero runtime importers; its `aws-sdk-bedrock` branch is unbuilt per its own docstring. Flipping `NEXT_PUBLIC_LLM_SDK` today changes nothing | `src/lib/llm-sdk-mode.ts:13-15`; `src/instrumentation.ts:76` snapshots the raw env value directly |
| `src/lib/admin-auth.ts` has zero runtime importers — `src/proxy.ts:11-13` deliberately re-implements the timing-safe compare with Web Crypto because Edge lacks `node:crypto`. The two must be kept in agreement or `/admin` can pass one and fail the other | `src/lib/admin-auth.ts`; `src/proxy.ts:11-13`, `:16` |
| `ArticleCard`, `Button` and `EmptyState` have zero importers anywhere in `src/` or `e2e/` — they compile but are unreferenced | `src/components/article-card.tsx`, `src/components/ui/button.tsx`, `src/components/ui/empty-state.tsx` |
| Five `voice-pitfalls` exports (`detectScreenReader`, `isIOS`, `voiceURIToGender`, `localeFallbackChain`, `normalizeVoiceURI`) have no production importer — only the two test files reference them | `src/components/chat/voice-pitfalls.ts` |
| `Graph` is exported from `src/components/hero-graph/scene.tsx:93` with a comment saying the physics variant embeds it, but no file imports `Graph` — `scene-physics.tsx` uses `HeroGraphInner` | `src/components/hero-graph/scene.tsx:93` |
| `@react-three/offscreen` and `@react-three/rapier` were declared prod deps with zero import sites anywhere in the repo — **both removed in v3.5.0**, so neither appears in `package.json` any more (33 prod / 17 dev; 3 packages removed, 0 added, 1 transitive downgrade). `@modelcontextprotocol/sdk` is still declared and still peer-only | `CHANGELOG.md:197-201`; `package.json:27` |
| `safeEmit`'s thenable-handling branch is dead code today because `emit` returns `void` | `src/lib/telemetry/with-trace.ts:98-101` |
| `budget.tick` is declared in `KIND_LITERALS` but no emit site was located | `src/lib/telemetry/schema.ts:44` |
| `accent: "in"` is declared in the voice type but no catalog entry uses it | `src/lib/voice-catalog.ts:27` |

### Cross-file string and shape contracts with no guard

| Observation | Cite |
|---|---|
| `src/app/admin/telemetry/page.tsx` hardcodes **7** Redis key literals — `anvilry:trace:${kind}` (`:25`), `anvilry:eval:latest` (`:217`), `anvilry:github:stats:latest` (`:251`), `anvilry:seo:audit:latest` (`:254`), `anvilry:content:audit:latest` (`:257`), `anvilry:health:latest` (`:260`), `anvilry:corpus:built_at` (`:265`) — that must match whatever the cron routes write | `src/app/admin/telemetry/page.tsx` |
| `TRACE_DELIMITER` is duplicated as a literal in the eval cron rather than imported from `@/lib/llm-trace`; changing the constant silently breaks the answer-text strip | `src/app/api/cron/eval/route.ts:95`, `:124-125` vs `src/lib/llm-trace.ts:23` |
| The 1-hour GitHub polling cadence now exists **only** in two fetch-level options; the segment `revalidate` was removed for `cacheComponents`. Deleting either silently changes the cadence | `src/app/api/github/stats/route.ts:31` and `src/lib/github.ts:101`, recorded at `src/app/api/github/stats/route.ts:3-7` |
| `SOURCE_LABELS` on `/articles` and the `ArticleSource` union in `platform-badge.tsx` must both stay in sync with the Velite `source` enum; a new platform without a label renders `undefined` in the filter pill | `src/app/articles/page.tsx:25-32`; `src/components/platform-badge.tsx:13-23`; enum `velite.config.ts:109` |
| `SkillTree` uses a non-null assertion `skills.find(s => s.group === groupName)!` against six hard-coded group names; renaming any group in `profile.ts` **throws at render**, while `GROUP_COLORS`/`catAbbrev` silently fall back | `src/components/game/skill-tree.tsx:155`, `:60-61`, `:27-34`, `:120-128` vs `src/lib/profile.ts:42-64` |
| `SkillTree`'s filter ids are derived by the same `col.group.replace(/[^a-z0-9]/gi, "-")` expression in five separate places | `src/components/game/skill-tree.tsx:230`, `:252`, `:372`, `:429`, `:449` |
| `boot-banner` string-replaces `"daily users" → "users"` and `"open-source repos" → "OSS repos"` to keep the line under ~60 chars; renaming those labels in `profile.ts` silently un-shortens and wraps the banner | `src/components/game/terminal/boot-banner.ts:38-40` vs `src/lib/profile.ts:36-40` |
| `SkeletonViewTransition`'s `h-[calc(100dvh-3.5rem)]` must track the `h-14` nav row | `src/components/ui/skeleton.tsx:102-126` vs `src/components/site-nav.tsx:42` |
| The `<img>` in the case-study diagram block and the `formatUpdated` UTC pin are hand-maintained; `about/page.tsx` also assumes `impactMetrics` is in Pensieve-then-AAVA order, and reordering the array silently swaps two numbers in the prose | `src/app/work/[slug]/page.tsx:123-135`; `src/app/about/page.tsx:10-14`, `:27`, `:40-42` |
| `home/hero.tsx`'s impact `<dl>` is `sm:grid-cols-3` to match exactly 3 `impactMetrics`; a 4th metric leaves a blank trailing cell (the comment records a prior `grid-cols-4` break) | `src/components/home/hero.tsx:62-64` |
| `featured-projects.tsx` hardcodes the CTA string `"View all 8 projects"` and does not read `allProjects.length` (which is 11) | `src/components/home/featured-projects.tsx:24` |
| `/stats` carries two hand-written string literals (`"2K+"` Pensieve, `"3K+"` AAVA) that are the only numbers on the page not derived from a single source; they must track `impactMetrics` by hand | `src/app/stats/page.tsx:11-12` |
| **No longer silent — fixed on this branch.** `src/app/mcp/page.tsx`'s `TOOLS` array is still a hand-maintained duplicate of the real tool set (`:35-45`), but it is now *enforced* rather than trusted: the array carries a comment naming it the public contract (`:32-34`), it lists all **9** registered tools (it had drifted to 7), and `src/app/mcp/tools-documented.test.ts` asserts the documented set and the route's `registerTool` calls are the same set — documents-nothing-extra (`:76`), documents-everything (`:81`), and identical-counts (`:90`), plus a regex-drop guard so a silent extraction failure cannot make the comparison vacuous (`:61`). Because `vitest run` is chained into `pnpm build`, adding a tool without documenting it now fails the build | `src/app/mcp/page.tsx:35-45`, contract comment `:32-34`; guard `src/app/mcp/tools-documented.test.ts:81`, `:90` |
| `global-error.tsx` duplicates the `globals.css` palette as inline hex literals (`#07080d`, `#e9ecf5`, `#9aa3b8`, `#747e99`, `#2c3346`, `#141826`, `#1f2433`) because CSS may not be applied when it fires; changing the palette silently desyncs this file | `src/app/global-error.tsx:14-18` vs `src/app/globals.css:8-31` |
| `dossier-card.tsx` renders two links to the same href but only the second calls `unlock("dossier-open")` — clicking the title does not record the discovery | `src/components/game/dossier-card.tsx:27` vs `:69-71` |
| `developer-rail.tsx` builds its LinkedIn label as `linkedin.com/in/{profile.githubUser}`, reusing the GitHub username rather than reading `profile.links.linkedin` | `src/components/game/developer-rail.tsx:77` |
| `developer-rail.tsx` always lists all five résumé PDFs, unlike the `resume` command which is gated by `NEXT_PUBLIC_RESUME_VARIANTS` | `src/components/game/developer-rail.tsx:35-47` vs `src/components/game/terminal/commands.ts:204-206` |

### Data-shape and sorting silences

| Observation | Cite |
|---|---|
| `pinnedProjects` requires **both** `pinned === true` and `pinRank != null`; a project marked pinned without a rank is silently dropped | `src/lib/content.ts:26-28` |
| Project `order` and `pinRank` both contain duplicates — order 5 (`ag-bash`, `tombstone`), 6 (`not-humans-lab`, `trelix`), 7 (`commandvault`, `inkforge`); pinRank 4 (`ag-bash`, `tombstone`), 5 (`contextos`, `trelix`). The sorts are plain numeric comparators, so tie order is whatever Velite's file walk produced — not pinned by frontmatter | `src/lib/content.ts:18-21`, `:26-28` |
| `inkforgeNotes` and `inkforgeArticles` both filter `allNotes`, not `allArticles`; the extra `!n.draft` in `inkforgeArticles` is redundant because drafts are already excluded | `src/lib/content.ts:56`, `:61`, `:49` |
| Notes and articles are sorted newest-first by **ISO string comparison**, not by `Date` | `src/lib/content.ts:50`, `:68` |
| `safeMs()` coerces an unparseable ISO date to `0` so `NaN` cannot poison the article group sort — malformed dates sort last, not randomly | `src/lib/article-grouping.ts:10-13` |
| `commits: 0` would be omitted from the corpus because the guard is truthiness, not null-check | `src/lib/corpus.ts:26-28` |
| `grpc-microservices` is the only project with no `commits`, and the only one where slug, `name` ("Order Processing System") and repo name (`gRPC-micro-services`) all differ | `content/projects/grpc-microservices.mdx:3-6` |
| `REPO_ALLOWLIST` is keyed on **GitHub repo names**, not slugs; it includes `Thunderboard-Labs` and `Shop.this` (no content file) and omits `Tombstone`, `trelix`, `Inkforge` (which have one). Casing must match the canonical repo name exactly | `src/lib/github.ts:28-40` |
| `src/lib/github.ts` is server-only **by convention, not enforcement** — it reads `process.env.GITHUB_TOKEN` and the docblock notes no `server-only` package is installed; the single server-component import site is the only guarantee | `src/lib/github.ts:6-10`, `:90` |
| `use-chat.ts`'s `commit()` writes a `reasoning` property that is not declared on the exported `ChatMessage` type, and no production code reads `message.reasoning`. **Resolved for this index:** `npx tsc --noEmit` exits 0 on the current tree, so this is not a type error and nothing catches it | `src/components/chat/use-chat.ts:190-192` vs the type at `:40-56` |
| `send`'s dep array includes `messages`, so its identity changes every message — callers must not memoize on it | `src/components/chat/use-chat.ts:384` |
| `useStt`'s Transcribe fallback is keyed on `transcribe.error`, so a *transient* failure sticks for the rest of the hook's life (`error` is cleared only inside `start()`) | `src/components/chat/use-stt.ts:25-27`; `src/components/chat/use-transcribe-recognition.ts:112` |
| `use-transcribe-recognition.ts` has **no** unmount cleanup effect, unlike its browser sibling; teardown happens only via `start` failure or `stop` | `src/components/chat/use-transcribe-recognition.ts` vs `src/components/chat/use-speech-recognition.ts:224` |
| `getServerSnapshot` in `discovery-store.ts` returns a fresh `new Set()` on every call, so the server snapshot is never referentially stable; `getDiscoveryCount()` reads module state **without** subscribing, so a component using only it never re-renders on unlock | `src/lib/discovery-store.ts:63`, `:86-88` |
| `clearHighlight()` must be used rather than nulling the module variable, otherwise the pending timer leaks and a stale timeout nulls a newer highlight | `src/lib/highlight-store.ts:40-47` |
| `inkTransitionRef` is a module-level mutable export assigned in an effect with **no** dependency array, so it re-assigns on every render | `src/components/ui/ink-transition.tsx:213-216` |
| `github-stats-strip.tsx`'s `stats!` non-null assertions are only safe because `fetchState === "ready"` implies `stats` was set in the same `.then` | `src/components/github-stats-strip.tsx:77-80` |
| `buildColumns()` in `SkillTree` runs unmemoized on every render | `src/components/game/skill-tree.tsx:353` |

### Rendering, styling and a11y silences

| Observation | Cite |
|---|---|
| `WebGLBoundary`'s fallback is a silent `return null` plus a `console.warn` hardcoded to the `[build-graph]` prefix — even though `voice-orb.tsx`, `hero-avatar/index.tsx` and `not-found.tsx` also use it. There is no reset path once `failed` is true, and `HeroAvatar` passes no `onFail` | `src/components/game/webgl-boundary.tsx:25`, `:30`; callers `src/components/hero-avatar/index.tsx:59`, `src/components/chat/voice-orb.tsx:46`, `src/app/not-found.tsx:26` |
| `HeroGraph` is **not** wrapped in `WebGLBoundary` and neither hero slot calls `useWebGLSupported()` | `src/components/hero-graph/index.tsx`; probe used only at `src/components/game/build-graph.tsx:29`, `src/components/chat/voice-orb.tsx:38` |
| The avatar's `frameloop="demand"` never settles, because `useAvatarIdle` calls `invalidate()` unconditionally every frame. The hero graph's rig by contrast only re-invalidates while `\|delta\| > 0.0006` — that threshold is the real settle condition | `src/components/hero-avatar/use-avatar-idle.ts:27` vs `src/components/hero-graph/scene.tsx:87` |
| Eye gaze is INERT with the shipped asset: `resolveRig` requires a `SkinnedMesh` whose name contains "head" **and** has a `morphTargetDictionary`, and `public/avatar/sairam.glb` is an Avaturn export with zero morph targets, so all 8 ARKit `setMorph()` calls are skipped. Bone-driven head/neck rotation and breathing still work | `src/components/hero-avatar/rig.ts:57-63`; `src/components/hero-avatar/avatar-mesh.tsx:97-109`; asserted deliberately at `src/lib/avatar-glb.test.ts:132-157` |
| A GLB bone rename leaves the avatar loading but never moving, with no error anywhere — `resolveRig` matches lowercased substrings and every field is nullable by design | `src/components/hero-avatar/rig.ts:43-63`, `:6-7` |
| The 3D hover label is commented out because drei/troika `<Text>` with no local `font` prop fetches font metadata from `cdn.jsdelivr.net` and throws an uncatchable unhandled rejection offline. `Node` still takes an unused `label` prop | `src/components/game/build-graph-scene.tsx:98-118`, `:49` |
| `scene-physics.tsx` contains **no** physics engine despite the filename and flag name — just `position.x/y/z = sin/cos(...)` set (not accumulated) each frame. It is also the only R3F file that bypasses the `@/lib/r3f` barrel | `src/components/hero-graph/scene-physics.tsx:42-44`, `:10-16`, `:4-6` |
| Under `NEXT_PUBLIC_GRAPH_PHYSICS=true` plus reduced motion, the canvas still renders continuously (`frameloop="always"`) — the early return is inside the frame callback, not at the Canvas level. The upstream gate normally prevents this from mounting | `src/components/hero-graph/scene-physics.tsx:20`, `:38`; gate `src/components/hero-graph/index.tsx:35` |
| `SkillTree` injects an **unscoped** global `* { animation: none !important; transition: none !important }` reduced-motion rule from inside the SVG | `src/components/game/skill-tree.tsx:571-579` |
| `globals.css` has **ten** separate `prefers-reduced-motion` blocks; any new animation needs its own. The orb's reduced-motion branch keeps `blur(1.4px) contrast(4.5)` and must never set `filter: none`, which shatters the metaballs | `src/app/globals.css:67`, `:84`, `:91`, `:99`, `:168`, `:226`, `:293`, `:315`, `:346`, `:380`; orb note `:173-177`, `:161-162` |
| `fmt.box` **silently slices** any content line longer than 56 chars — long URLs or tech lists inside `cat`/`contact`/`statsBox` get cut, not wrapped | `src/components/game/terminal/fmt.ts:13`, `:67` |
| `AnvilCoreSurface` renders the **raw** assistant content through `MarkdownMessage` rather than `toCaptionText`, unlike TalkMode's caption path — card tokens are not stripped there | `src/components/chat/anvil-core-surface.tsx:163` vs `src/components/chat/talk-mode.tsx:263` |
| `AnvilCoreSurface` imports `MarkdownMessage` **statically** while the other two surfaces lazy-load it, so that surface pulls react-markdown into its chunk | `src/components/chat/anvil-core-surface.tsx:6` vs `src/components/chat/chat-messages.tsx:259-262` |
| `OrbMesh`'s `uniforms` `useMemo` has an empty dep array with an eslint disable, so a change to `errorMode` after mount will not rebuild `uErrorMode` | `src/components/chat/voice-orb-3d.tsx:217-218` |
| `graph-data.ts` positions are constrained by an inline frustum budget ("camera z=7, fov=45 → visible half-height ≈ 2.9 / SCALE=1.6 ≈ 1.8 units"); coordinates outside roughly ±1.8 on Y clip with no error | `src/lib/graph-data.ts:37` |
| `ptr` in the hero graph is a module-level mutable singleton, so two simultaneously mounted `Graph` instances would share pointer state | `src/components/hero-graph/scene.tsx:15`, `:11-14` |
| `GlowFallback` in `hero-avatar/index.tsx` intentionally duplicates the two blurred circles from `hero-graph/index.tsx:43-44` so switching hero modes causes no layout shift — the two must be kept in sync by hand | `src/components/hero-avatar/index.tsx:16-18`, `:19-26` |
| Unknown `NEXT_PUBLIC_AVATAR_POSITION` values silently render the `hero-top` layout rather than erroring | `src/components/hero-avatar/index.tsx:102-110` |
| `graph-index.tsx` promotes the **last** card to `sm:col-span-2` when a group has an odd node count — a layout rule keyed off `g.nodes.length % 2` | `src/components/game/graph-index.tsx:26` |
| `mobile-nav.tsx`'s focus trap queries only inside `panelRef`; adding focusable chrome outside the panel while open escapes the trap | `src/components/mobile-nav.tsx:24-50` |
| `easter-eggs.tsx`'s dismiss button is explicitly sized `h-7 w-7` because Tailwind preflight zeroes button padding and it would otherwise collapse to ~15 px (WCAG 2.5.8) | `src/components/game/easter-eggs.tsx:139-142` |
| Tailwind v4 here has no `tailwindcss-animate`, so `animate-in` utilities silently no-op — `.hero-rise` is reused instead | `src/components/game/easter-eggs.tsx:131-133` |
| `:focus-visible:not(.no-focus-ring)` is a deliberately **unlayered** rule that beats any Tailwind utility regardless of specificity; `.no-focus-ring` is the only opt-out | `src/app/globals.css:194-200` |
| `Reveal`'s `!mounted` branch is the no-JS safety net — removing it makes every below-fold section permanently invisible whenever hydration fails | `src/components/ui/reveal.tsx:28`, `:8-15` |
| Each standalone page must pass `titleAs="h1"` to its first `<Section>`, or the page's highest heading is an `h2` | `src/components/ui/section.tsx:4-11` |
| Command-palette actions whose **label** mutates must pin a stable `value` or cmdk re-scores mid-interaction | `src/components/command-palette-content.tsx:321-323` (`copy-email`), `:454-455` (`voice-tts`), plus four other voice toggles |
| Recent palette items need a `valuePrefix="recent"` so their cmdk values do not collide with the canonical copies | `src/components/command-palette-content.tsx:656`, `:719` |
| `VoicePicker`/`VoiceSettingsDialog` are siblings of the palette dialog, opened only after it closes, to avoid two stacked Radix focus traps | `src/components/command-palette-content.tsx:678-681` |
| `SegmentedControl` hand-rolls `role="radiogroup"`/`role="radio"` on buttons — no Radix RadioGroup, so arrow-key roving is not provided | `src/components/chat/voice-settings-dialog.tsx:111-133` |

### Operational silences

| Observation | Cite |
|---|---|
| `src/app/api/cron/github-sync/route.ts:8` docstrings itself "Hourly" but `vercel.json` schedules it `0 8 * * *` (daily); its Redis TTL is `ex: 5400` (90 min), so the `cache_fresh` idempotency guard at `:29-32` is effectively always cold | `src/app/api/cron/github-sync/route.ts:8`, `:29-32`, `:55`; `vercel.json:3-7` |
| Telemetry trimming is piggybacked on writes — a kind that stops receiving events is never trimmed again | `src/lib/telemetry/emit.ts:42`, `:58-80` |
| `scripts/replay-trace.mjs`'s `KINDS` array is hardcoded, so a new span kind is invisible to the replay CLI until listed. Its docblock also records a fixed bug: `@upstash/redis` auto-deserializes, so a `JSON.parse(m)` would coerce members to `"[object Object]"` and silently drop every event | `scripts/replay-trace.mjs:47-55`, `:65-68` |
| `/api/error`'s post-read 413 backstop exists because `sendBeacon` does not always send `Content-Length`, making the header-only gate bypassable | `src/app/api/error/route.ts:127-133` |
| `componentStack` is **not** redacted (documented as React-internal, not visitor-supplied) while `message` and `stack` are | `src/app/api/error/route.ts:145-166` |
| `health-check`'s `p2_pass` is computed (`:183-185`) and reported (`:200`) but does **not** feed `topStatus`, which reads only `p1Pass` plus the fail/warn name lists | `src/app/api/cron/health-check/route.ts:183-185`, `:187-191`, `:200` |
| `content-audit`'s `EIGHTEEN_MONTHS_MS` is 18 × **30-day** months = 540 days, not calendar months | `src/app/api/cron/content-audit/route.ts:16` |
| `seo-audit` must map Project's summary field to `excerpt`; using `summary` false-positives every project | `src/app/api/cron/seo-audit/route.ts:47-54` |
| The eval cron calls the **live** `/api/chat`, so it consumes the same per-IP rate limit and real Bedrock spend (~12 calls ≈ $0.012/run) | `src/app/api/cron/eval/route.ts:16`, `:113`, `:117` |
| `/api/visit` returns the current total with `today: 0` on a limiter denial rather than a 429, so the badge never breaks — and `total === 0` is treated as "Redis was down" and never cached | `src/app/api/visit/route.ts:54-59`; `src/components/site-footer.tsx:41-53`, `:67` |
| The `.md` passthrough routes read the **filesystem at request time**, so `content/` must ship in the deployed bundle; they use Velite only for the existence check. Two parallel implementations serve the same URLs | `src/app/work/[slug].md/route.ts:6-9`; `src/app/api/md/work/[slug]/route.ts:11-32`; rewrites `next.config.ts:220-228` |
| `robots.txt` has **no** `disallow` entries — `/admin/*` is not excluded from crawling, only protected by the proxy | `src/app/robots.ts:5-6` |
| The RSS feed ignores `NOTES_ENABLED`/`ARTICLES_ENABLED`, unlike `sitemap.ts` | `src/app/feed.xml/route.ts:5` vs `src/app/sitemap.ts:41`, `:62` |
| `articles/[slug]/generateStaticParams` filters guaranteed-404 slugs while the sibling `opengraph-image.tsx` maps **all** slugs unfiltered — the disagreement is intentional, so OG images exist for slugs whose pages redirect or 404 | `src/app/articles/[slug]/page.tsx:39-45` vs `src/app/articles/[slug]/opengraph-image.tsx:28-30` |
| `sitemap.ts` never emits `lastModified` on any entry | `src/app/sitemap.ts` |
| `Makefile`'s `resume-list` parses `ls -lh` columns 9 and 5 — filenames containing spaces misalign | `Makefile:359` |
| `make new-project` hardcodes the GitHub user `sairam0424` into the scaffolded `repo:` field | `Makefile:131` |
| There is **no `make e2e` target** — Playwright is reachable only via `pnpm e2e`/`pnpm e2e:ui` or the CI job | `Makefile` (verified absent, [11 § Makefile targets](./11-config-build-ci-infra.md#makefile-targets)) |
| Actual PDF text extraction under `pdfjs-dist@6.2.108` was explicitly flagged NOT VERIFIED in the release commit — build and typecheck pass but the extraction path was never exercised post-bump | `src/components/chat/file-picker-button.tsx:77-104`; commit `2f309d2` per section 13 |
| The `/api/error` `console.warn` breadcrumb and `anvilry:errors:recent` write are fire-and-forget with `.catch(() => {})` | `src/app/api/error/route.ts:174-180` |
| `easter-eggs.tsx`'s console greeting is guarded by a module-level `let consoleGreeted` with no storage, so a reload re-prints it despite the "once per session" wording | `src/components/game/easter-eggs.tsx:31`, `:57-66` |

### Modules with no direct unit-test guard

The following have no dedicated test file, so a behavioural change in them produces no test failure:
`src/lib/article-grouping.ts`, `src/lib/llms-txt.ts`, `src/lib/resume-json.ts`, `src/lib/flags.ts`,
`src/lib/writing-flags.ts`, `src/lib/enabled-views.ts`, `src/lib/discovery-store.ts`,
`src/lib/highlight-store.ts`, `src/lib/utils.ts`, `src/lib/rate-limit.ts`, `src/lib/redis.ts`,
`src/lib/r3f.ts`, `src/lib/use-media-query.ts`, `src/lib/use-mounted.ts`, `src/lib/use-reduced-motion.ts`,
`src/lib/scroll/types.ts`, `src/lib/scroll/use-auto-scroll.ts`, and all of
`src/components/hero-graph/**` (zero in-directory tests). Cites: the per-module test map at section 03
("Test guards") and section 04; `src/lib/scroll/resolve-flag.test.ts` is the only scroll unit test besides the
two engine DOM suites.

---

## Documented-but-unconfirmed

Every row below **whose claim is not struck through** is a doc claim that the code contradicts. Both cites
given. Plan and spec files under `docs/superpowers/` are excluded as dated point-in-time artifacts.

**Seventeen rows are struck through.** Those claims were *corrected on this branch* — twelve by the
`CLAUDE.md` / `ARCHITECTURE.md` / `DEPLOY.md` rewrite, three by in-code comment fixes (`graph-data.ts`,
`game-model.ts`, `scene.tsx`), one (`agent-trace`) on both sides at once, and one (`docs/README.md`) by
this index PR itself. They are retained as history because they are what sent earlier readings of this
codebase wrong, but the cite now resolves to the
corrected text, not to the defect. **Do not read a struck-through row as a live contradiction — the work is
already done.** Rows whose doc side was re-checked against the rewritten `CLAUDE.md` / `ARCHITECTURE.md` /
`DEPLOY.md` and found **still wrong** carry an explicit "still live" flag in the claim column; unstruck rows
citing any other file were untouched by this branch and stand as written.

Every `CLAUDE.md` line number in this table was re-derived against the rewritten file with `sed -n`.
`CLAUDE.md` went from 355 to 406 lines (+70 / −19) and the drift was *not* uniform — earlier drafts were low
by 0-1 lines before the command list, ~5-9 around the route tree, ~19-21 through the content and MCP
sections, and ~29-49 from the 3D notes onward. Never assume a fixed offset; re-derive.

> **STALE AS OF THE BUNDLE-BUDGET CHANGE — the `CLAUDE.md` cites *in this table* need one more pass.**
> The bundle work moved `CLAUDE.md` again, from **406 to 415 lines**, in two insertions: the `pnpm analyze`
> command block near the top and a rewritten CI-pipeline section (`CLAUDE.md:92-100`). The resulting offset
> *is* uniform in the region this table touches — **every cite at old line 92 or above needs +9** (verified
> line-by-line against `git show HEAD:CLAUDE.md`); old 19–85 needs +6; old 1–18 is unchanged; old 86–91 (the
> CI-pipeline bullet list) was *rewritten*, so `codeql.yml`/`dependency-review.yml` land at +5 and the two
> lines that named `bundle-analysis.yml` have no successor at all — re-read that block, never offset it.
> The `CLAUDE.md` cites *outside* this table were corrected in place on this branch:
> `:332` (AWS_REGION), `:356` (register policy),
> `:334`/`:337`/`:342` (base URL), `:38-39` (search-index), `:92`/`:93` (CI pipeline). Everything still
> numbered below is pre-shift. Re-derive with `sed -n`, not by adding 9 on faith — and note that the
> **context-relative** cites in these rows (`` `:250` ``, `` `:213-214` ``, `` `:293` `` …) shift with their
> owning file, which is not always `CLAUDE.md`.

| Doc claim | Doc cite | What the code says |
|---|---|---|
| ~~"The Four-View System" / four views~~ — **corrected on this branch** | `CLAUDE.md:108-116`; `ARCHITECTURE.md:17` | The heading is now "### The View System" (`CLAUDE.md:115`) and the prose states outright that the `View` union has **six** members, with a per-view "Switcher pill?" column covering `voice` (desktop only, post-hydration) and `resume` (never) (`CLAUDE.md:108-108`). `ARCHITECTURE.md:17` no longer says "4-view system" either — it now spells out the six members and the 4-pills-server-side / 5-on-desktop split. The code never changed and was always six: `"classic" \| "gamified" \| "chat" \| "developer" \| "voice" \| "resume"` (`src/components/view-context.tsx:24`, `VIEWS` at `:26`), `ViewRouter` branches on all six (`src/components/view-router.tsx:58-69`), and `src/components/view-switcher.tsx:16-21` renders only 4 pills server-side, appending Voice post-mount (`:38`) |
| ~~Terminal has "~16 commands"~~ — **corrected on this branch, all three places** | `CLAUDE.md:115`; `ARCHITECTURE.md:74`; `README.md:10` | All three now say **31**. `CLAUDE.md:115` reads "Keyboard-driven terminal with **31** commands (27 visible + 4 hidden eggs)", `ARCHITECTURE.md:74` reads "Developer terminal view (31 commands, combobox)", and `README.md:10` was changed from "~16 commands" to "31 commands". That matches the registry: 27 visible + 4 hidden eggs (`secret`, `personal`, `uses`, `now`) filtered out of `COMMAND_NAMES` (`src/components/game/terminal/commands.ts:503-508`, `:525-527`) |
| ~~`/mcp` is `(force-static)`~~ — **corrected on this branch** | `CLAUDE.md:140` | The route-tree line now reads "`/mcp`  MCP server documentation (no segment config — see note below)". That matches the code, which never changed: `src/app/mcp/page.tsx` exports no route segment config, and a grep for `force-static` across all non-API files under `src/app` returns nothing |
| ~~Route tree lists one cron route~~ — **corrected on this branch** | `CLAUDE.md:156-157` | The tree now collapses to `/api/cron/{eval,health-check,github-sync,seo-audit,content-audit}` with the annotation "5 crons, ALL fail-closed on CRON_SECRET (vercel.json:3-7)", and `CLAUDE.md:164` adds the four `/api/md/*` handlers that were also missing. Matches `vercel.json:3-7`: `health-check` `0 5 * * *`, `eval` `0 9 * * 1`, `github-sync` `0 8 * * *`, `seo-audit` `0 6 * * 1`, `content-audit` `0 7 * * 1` |
| ~~"Every API route runs on the Node.js runtime … with a 30s max duration"~~ — **corrected on this branch** | `CLAUDE.md:162-181` | That sentence is gone. `CLAUDE.md:162` now leads with "**Runtime & duration — do not add `export const runtime`.**", explains that the export was removed because `cacheComponents: true` rejects its mere presence, and `:154-162` is a per-route `maxDuration` table. It matches the code: no `export const runtime` survives anywhere in `src/` — the only hit is a removal comment (`src/app/api/mcp/[transport]/route.ts:6-8`) — and `maxDuration` is per-route: 60 (`cron/eval`, `cron/seo-audit`, `cron/content-audit`), 30 (`chat`, `mcp`, `cron/github-sync`), 25 (`cron/health-check`), 20 (`transcribe`), 15 (`tts`, `tts-google`), 5 (`error`) |
| ~~`.md` notes come from Inkforge and carry extended frontmatter; hand-written `.mdx` notes omit those fields~~ — **corrected on this branch** | `CLAUDE.md:190-192` | Rewritten to the schema-level truth ("the Velite schema makes the extended Inkforge frontmatter … optional"), plus an explicit new paragraph at `:182`: "**the extension does not indicate provenance** … all five notes carry `generatedBy: inkforge`, including both `.mdx` ones … Do not infer 'hand-written' from `.mdx`." Matches the code — `src/lib/content.ts:56`, `:61` filter on `generatedBy`, not on extension |
| In-context corpus is "~4KB" — **still live** | `CLAUDE.md:206` vs `CHANGELOG.md:434` calling `/llms-full.txt` the "full chatbot corpus (~6KB)" | Both figures come from prose; `buildCorpus()` output (`src/lib/corpus.ts:13`) was not measured. Which is current, or whether they describe different strings, is unresolved |
| ~~MCP server exposes "**7 tools**"~~ — **corrected on this branch, and one half of this row was never true** | `CLAUDE.md:212`, `:293`; `src/app/api/mcp/[transport]/route.ts:22`; `src/app/mcp/page.tsx:35-45` | Two things. (1) The **count is fixed everywhere**: `CLAUDE.md:212` says "**9 tools**" and its table gained the two missing rows at `:213-214`, the Key-Files row at `CLAUDE.md:303` says "MCP server (9 read-only tools)", the route docblock at `src/app/api/mcp/[transport]/route.ts:22` says "9 read-only tools", and `src/app/mcp/page.tsx:35-45` lists all 9. The route registers `list_all_content` (`route.ts:98-99`) and `get_content_item` (`:108-109`) on top of the original seven; the 7→9 growth is recorded at `CHANGELOG.md:455-456`. **Guard:** `src/app/mcp/tools-documented.test.ts` asserts the page's `TOOLS` array and the route's `registerTool` names are identical, and `vitest run` is chained into `pnpm build` (`package.json:11`), so an undocumented tool now fails the build. (2) **The claim that `src/lib/mcp-tools.ts:5-13` "says the same" was an error in this index, not a defect in the code** — `grep -nE '7 tools\|seven' src/lib/mcp-tools.ts` returns nothing, and never did; that docblock carries no tool count at all |
| Voice settings live in `src/lib/voice-settings.ts` — **still live** | `CLAUDE.md:249` | That path does not exist. The real module is `src/lib/voice-settings-context.tsx` (also recorded at [04 § `src/lib/voice-settings-context.tsx`](./04-lib-ai-voice-infra.md#srclibvoice-settings-contexttsx)) |
| ~~"`@react-three/offscreen` for worker offload"~~ — **corrected on this branch, then overtaken by the v3.5.0 removal: `CLAUDE.md` is now stale in the other direction** | `CLAUDE.md:259-260` | The worker-offload claim is gone: `:249` heads a "**Two declared dependencies are not actually used**" section, `:250` says of `@react-three/offscreen` "no worker/OffscreenCanvas anywhere; the 'worker offload' this file previously claimed does not exist", and `:251` says the same for `@react-three/rapier`. That was accurate while both packages were declared. **v3.5.0 removed both** (`CHANGELOG.md:197-198`), so the section now describes dependencies that are not declared at all — and its own parenthetical "(`package.json`, but zero imports in `src/`)" at `:249` points at a file where neither name appears. The code half is unchanged and still true: zero import sites in `src/`, and a repo-wide grep for `offscreen`/`Offscreen` now matches nothing outside `pnpm-lock.yaml`'s unrelated `@types/offscreencanvas` and the docs |
| ~~Update the base URL in "exactly four files"~~ — **corrected on this branch, both docs** | `CLAUDE.md:335-351`; `DEPLOY.md:146-158` | `CLAUDE.md:335` now opens "hardcoded in **19 files / 25 occurrences**, not four", gives the grep at `:328`, calls out `src/lib/mcp-tools.test.ts:69` as one of the 19, breaks out the 7 hits in `json-ld.tsx` at `:333`, and closes "Run the grep rather than trusting this list". `DEPLOY.md:146-158` step 3 was rewritten the same way. **24 occurrences across 18 non-test files** — see the base-URL table above, which excludes the test file and is otherwise the same set |
| ~~`ask-portfolio.dom.test.ts` covers prompt injection and XSS on streamed markdown~~ — **corrected on this branch** | `CLAUDE.md:365-366` | The bullet was replaced outright: `:356` now names the real gate — "**The prompt-injection / XSS guard is `src/components/chat/parse-cards.test.ts`**" — and enumerates what it pins (`:41` unknown slug dropped, `:50` malformed kind ignored, `:55` slug charset locks out injected hrefs, `:67` raw HTML stays inert text). `:357` explicitly disclaims the old file: `src/components/ask-portfolio.dom.test.tsx` (note the `.tsx`) "is **not** this guard. It has exactly two tests: shared-transport streaming (`:46`) and the 503 not-configured message (`:70`), and contains zero injection or XSS assertions" |
| ~~`agent-trace.test.ts` "blocks shipping" while `PLACEHOLDER_SENTINEL` remains~~ — **corrected on this branch, both sides** | `CLAUDE.md:368`; banner `src/lib/agent-trace.ts:13-16` | The claim was false and is now gone from both places. The banner used to say the test "BLOCKS shipping"; it now says the opposite in the same words as the code — "It does **NOT** block the build: while the sentinel remains, `traceApproved` is false and `glass-box-demo.tsx` renders NOTHING, so drafts never reach a visitor" (`src/lib/agent-trace.ts:13-16`), which agrees with `:118` ("NOT a hard build failure", verified verbatim) instead of contradicting it. `CLAUDE.md:368` was rewritten to match and now records the old wording as the origin of this doc's error. The behaviour is unchanged: the assertion is `expect(traceApproved).toBe(!hasSentinel)` — a consistency check that passes in **both** states (`src/lib/agent-trace.test.ts:56`); the sentinel is still present, `traceApproved === false`, and `src/components/game/glass-box-demo.tsx:40` returns `null`, so the demo is dark and nothing is blocked |
| ~~`pnpm search-index`~~ — **corrected on this branch** | `CLAUDE.md:38-39` | The bare `pnpm search-index` line is gone. `:38` is now a `# NOTE:` stating that this is a Makefile target only and that no such pnpm script exists; `:39` is `make search-index` with the underlying `pnpm pagefind` invocation inline. Still no such script in `package.json:8-22` (12 scripts); the only Pagefind entry point remains `Makefile:64-66`, and `docs/configuration.md:132` always said `make search-index` |
| `pnpm dev` "starts Velite watch + Next.js dev" — **still live in all three places** | `CLAUDE.md:14`; `README.md:45`; `.claude/skills/dev-local/SKILL.md:29` | `package.json:9-10` is `"predev": "velite"` (one-shot) + `"dev": "next dev"` — no `--watch`, and no `&` background job of the kind `SKILL.md:29` describes. `CLAUDE.md:188` separately and correctly describes `predev` as synchronous, as does `SKILL.md:30`. (The dev watcher does exist, but inside `next.config.ts:12-16`, not in the script) |
| `ci.yml` runs "lint → typecheck → `pnpm test`" — **still live, but narrowed on this branch** | `CLAUDE.md:93`; the workflow count above it at `:92` | Those three are the steps of the `ci` **job** only, and `ci.yml` holds **four** jobs: `ci` (`.github/workflows/ci.yml:10`), `e2e` (`:68-122`), `install-pnpm-11` (`:124`) and `security-alerts` (`:173`). `e2e` and `security-alerts` were added in v3.4.2 (`CHANGELOG.md:238-242`); `install-pnpm-11` came later, with the `pnpm-workspace.yaml` migration. The `ci` job also runs a fourth step the claim omits — the index-citation check at `:65-66`. **What this branch fixed:** the same bullet now names the `e2e` job's bundle budget gate, and `CLAUDE.md:92` says "three workflows", which became correct only when `bundle-analysis.yml` was deleted. What is still missing is the job list, not the workflow list |
| `EXTENDED_THINKING` listed among `NEXT_PUBLIC_*` flags | `ARCHITECTURE.md:99` | The server toggle is the non-prefixed `process.env.EXTENDED_THINKING` (`src/app/api/chat/route.ts:263`); a separate client flag `NEXT_PUBLIC_EXTENDED_THINKING` is read at `src/components/chat/chat-messages.tsx:159`. Which name `ARCHITECTURE.md` intends is unconfirmed |
| ~~Bedrock chain is Opus-primary / Sonnet-secondary; direct chain is opus-4-7 first~~ — **corrected on this branch** | `DEPLOY.md:92-103` | Both chains were inverted in `DEPLOY.md` and both are now right: the table at `:94-95` lists `us.anthropic.claude-sonnet-4-6` as Primary and `us.anthropic.claude-opus-4-6-v1` as Secondary, `:99` reads `claude-sonnet-4-6 → claude-opus-4-7 → claude-haiku-4-5`, and a new `:101-103` states "Both chains are **Sonnet-primary**, not Opus-primary — Opus is the escalation tier, not the default" and defers to `src/lib/llm.ts` as authoritative. Matches `src/lib/llm.ts:31-35` (verified verbatim). `CLAUDE.md:200` and `docs/configuration.md:43-46` had it right all along |
| Build command "runs `velite --clean && next build`" — **still live; `DEPLOY.md:20` was not touched by this branch's rewrite** | `DEPLOY.md:20` | `package.json:11` is `velite --clean && vitest run && next build` (verified verbatim). Dropping the test step from the description undercuts the very gate `CLAUDE.md:372` relies on ("Tests run as part of `pnpm build` — a failing test blocks deployment"), and the same chained `vitest run` is what makes the new `tools-documented` / `client-ip-consistency` / `health-expectations` / `manifest` / `llms-txt` guards build-blocking |
| "a Next.js frontend with three API routes (`/api/chat`, `/api/tts`, `/api/transcribe`)" | `SECURITY.md:5` | `src/app/api/` has eleven entries: `chat`, `cron` (×5), `error`, `github`, `mcp`, `md` (×4), `resume.json`, `transcribe`, `tts`, `tts-google`, `visit` |
| TTS engines are `"browser"` / `"polly"` | `VOICE.md:156`, `:373` | `src/lib/voice-settings-context.tsx:29` is `export type TtsEngine = "browser" \| "polly" \| "google";`. `VOICE.md` §7 (`:873`) and `docs/configuration.md:240` both describe the Google engine, so only §1/§3 are stale. Same section, `VOICE.md:162` treats "Joanna" as a fixed property, superseded by the 18-voice catalog |
| CSP "shipped as Report-Only first … to be promoted to enforced" | `next.config.ts:19-20` | Already enforced — the header key at `next.config.ts:94` is `Content-Security-Policy`. `:84-93` is the accurate note |
| Banner is "hidden via CSS (h-0) when the flag is off so there is zero layout shift" | `src/components/open-to-work-banner.tsx:6-8` | `src/app/layout.tsx:99` conditionally omits the element entirely. The code does nothing of the kind |
| `ViewEscapeHatch` is "auto-rendered by view-router for non-classic views" | `src/components/home/resume-view.tsx:12-14` | `src/components/view-router.tsx` renders no escape hatch and `ResumeView` renders none either, so the resume view has no in-page back-to-Classic control. Each other view imports it itself (`chat/anvil-view.tsx`, `chat/chat-view.tsx`, `game/game-view.tsx`, `game/developer-view.tsx`) |
| `project-card.tsx` subscribes to the highlight store via `useSyncExternalStore` | `src/lib/highlight-store.ts:9-10` | `project-card.tsx` does not import the module; nothing subscribes |
| "enlarged reactive orb (~200px)" | `src/components/chat/anvil-core-surface.tsx:20-22` | The implementation renders a CSS-only `anvil-orb-idle` blob at `h-16 w-16` (`:134-138`) |
| Console greeting is "once per session" | `src/components/game/easter-eggs.tsx:57-66` | The only guard is a module-level `let consoleGreeted` (`:31`) with no sessionStorage/localStorage; a reload re-prints |
| "Reset when the scenario changes" | `src/components/game/use-trace-runner.ts:69-70` | `reset`'s identity is stable (it depends only on `clearTimers`, `[]` deps), so the effect cleanup fires on unmount only. Scenario changes are reset explicitly by the `<select>` `onChange` at `src/components/game/glass-box-demo.tsx:59` |
| "Hourly GitHub stats cache warm" | `src/app/api/cron/github-sync/route.ts:8` | `vercel.json` schedules it `0 8 * * *` — daily |
| ~~"the 5 flagship work systems + 8 OSS repos"~~ — **corrected on this branch** | `src/lib/graph-data.ts:3-4` | The stale `8` is gone. The docblock is now de-numbered — "Nodes = every flagship work system + every OSS repo (see `graphNodes` below for the count — game-model.ts asserts a bijection with real content, so it moves with the content)" (`src/lib/graph-data.ts:3-4`) — so it cannot go stale again. The array still holds 5 work + **11** project nodes (`:18-41`) |
| ~~"3 of the 10 graph node ids"~~ — **corrected on this branch** | `src/lib/game-model.ts:19` | The comment now reads "3 of the **16** graph node ids" (`src/lib/game-model.ts:19`), matching `graphNodes` and `NODE_CONTENT`, which both hold 16 entries. The same edit also replaced the derived-and-equally-stale "would 404 for 30% of the graph" with "would 404 for those three" (`:21-22`) |
| ~~"All 10 nodes as ONE InstancedMesh"~~ — **corrected on this branch** | `src/components/hero-graph/scene.tsx:17-18` | De-numbered rather than re-numbered: the comment now reads "Every node as ONE InstancedMesh — single draw call (research-verified). Count is taken from `graphNodes.length`, so adding content never adds a draw call" (`src/components/hero-graph/scene.tsx:17-18`), which is what the code always did (`graphNodes.length`, `scene.tsx:45`). `src/lib/graph-data.ts:18-41` defines 16 nodes |
| ~~"`docs/` — *(none yet — add docs here as they accumulate)*"~~ — **corrected by this index PR** | `docs/README.md:38-43` | The placeholder row was replaced with the real inventory: `configuration.md`, `index/` (this directory), `superpowers/plans/` (14 dated plans) and `superpowers/specs/` (5 dated specs) — `docs/README.md:40-43` |
| "Notes content lives here … Empty by design — the /notes section ships dark until real posts exist" | `content/notes/.gitkeep` | Five notes are shipped. The real dark/live gate is `NOTES_ENABLED` (`src/lib/writing-flags.ts:22-23`) |
| "Newest at the **BOTTOM**" | `.claude/skills/new-loop/references/LOG.md:5` | The repo's own `LOG.md:4` says "Newest first. Append an entry **above** older entries" and its entries are newest-first. Following the template verbatim appends in the wrong direction |
| Native articles "render their body inline like notes" | `velite.config.ts:87-92` | All 15 Article files are frontmatter-only (`body: ""` in `.velite/articles.json`), including the single `source: native` file, so that path is unexercised |
| `ARCHITECTURE.md` version marker `v1.0.0` | `ARCHITECTURE.md:8` | Versions the knowledge-base model, not the app (which is `3.6.0`, `package.json:3`). `CHANGELOG.md:859-867` likewise has link references only for `1.0.0`–`1.6.0` |

---

## UNVERIFIED

Carried forward honestly from the 13 section indexes. **Three** items are resolved and marked — two for this
page (`tsc --noEmit`, and the `three.js` single-copy / stays-lazy claim, now measured), one by the code fixes
on this branch (the `GET /api/mcp/mcp` status). Two further items are struck as *superseded* rather than
verified: the `pnpm.overrides` resolved versions (recorded, but second-hand) and the two unused R3F packages
(resolved by deletion, not by documentation).

- **RESOLVED for this page.** The undeclared `reasoning` property written at `src/components/chat/use-chat.ts:190-192` is **not** a type error: `npx tsc --noEmit` exits 0 on the current tree (run for this index). Nothing catches it.
- Render-mode / revalidate / prerendered-slug values quoted anywhere in this index came from a local, gitignored build (`.next/prerender-manifest.json`, `.next/routes-manifest.json`, dated 15 Aug) and reflect that machine's `.env.local` flag values. Segment-config facts (`instant`, `"use cache"`, `cacheLife`, `generateStaticParams`, `generateMetadata`) are read from source and are env-independent.
- Whether `/articles/<slug>` routes that call `redirect()` emit a prerendered page artifact. Several slugs have an `opengraph-image` manifest entry but no page entry — consistent with the redirect path, not proven.
- Which implementation actually serves a live `/work/<slug>.md` request: the bare-array (`afterFiles`) rewrite at `next.config.ts:220-228` or the filesystem handler at `src/app/work/[slug].md/route.ts`. Resolution order was not exercised against a running server. Both produce byte-identical output.
- That `src/proxy.ts` actually executes on the Edge runtime. Its own docblock (`:5`) and `CLAUDE.md` assert Edge, the code uses only Web Crypto + `atob`, and no Node-proxy opt-in exists in `next.config.ts` (verified absent) — but the runtime Next 16 assigns was not observed.
- **RESOLVED on this branch, and it was a live defect.** A bare `GET /api/mcp/mcp` does **not** return 200 — it returns **405** (`{"jsonrpc":"2.0","error":{"code":-32000,"message":"Method not allowed."},"id":null}`), unconditionally, as the first branch of mcp-handler's streamable-endpoint handler and independently of this project's `disableSse` setting. Verified live: `GET /api/mcp/mcp` → 405, `GET /api/mcp/sse` → 404, `POST /api/mcp/mcp` with a JSON-RPC `initialize` → 200 (`src/lib/health-expectations.ts:8-16`). **But `mcp_get` was NOT failing on every run — it was FALSELY PASSING**, because of a second and deeper defect on the same path. The cron probed `VERCEL_URL`, the per-*deployment* host, and this project has Vercel deployment protection enabled for everything except custom domains — so every probe 302'd to Vercel SSO, and because `fetch` follows redirects by default the probe read **200 with ~478 KB of `vercel.com/login` HTML** and scored an auth wall as a healthy app, having never reached the app at all. That is why 11 of the 13 checks, `mcp_get` included, were green on a wall (`src/lib/health-expectations.ts:37-45`; the measurement and the correction are stated outright in the test docblock at `src/lib/health-expectations.test.ts:64-71` — "mcp_get was FALSELY PASSING, not failing"). The "405 fails a blanket `!== 200` gate on every run" story is the *counterfactual against the production alias*, which is what the source docblocks at `src/lib/health-expectations.ts:21-22` and `src/app/api/cron/health-check/route.ts:100` describe; it is not what was happening in production. Both halves are fixed. The base now comes from `probeBase()`, which prefers `VERCEL_PROJECT_PRODUCTION_URL` over `VERCEL_URL` (`src/lib/health-expectations.ts:51-58`, consumed at `src/app/api/cron/health-check/route.ts:152` — the route no longer reads `VERCEL_URL` itself), and `probe()` sets `redirect: "manual"` and fails any 3xx, naming Vercel SSO when it recognises the destination (`src/app/api/cron/health-check/route.ts:78`, guard `:86-98`). Expected status is now per-check rather than a blanket 200: the override table lives at `src/lib/health-expectations.ts:30-32` (`mcp_get: 405`) and the route consults it at `src/app/api/cron/health-check/route.ts:103` (`if (!isExpectedStatus(check.name, http_status))`), with the `mcp_get` CHECKS row at `:61`. Caveat recorded in source (`src/lib/health-expectations.ts:24-27`): the 405 is mcp-handler's behaviour, not ours, so `src/lib/health-expectations.test.ts:161` pins the installed `mcp-handler` version to make a future major bump fail the suite rather than silently re-break the check. What is still unverified is only the stronger claim — that the server completes a full MCP session; this remains a liveness check, not a functional one (`src/lib/health-expectations.ts:18-22`).
- Whether Vercel Cron actually injects `Authorization: Bearer ${CRON_SECRET}` automatically. The claim comes from the route docstring (`src/app/api/cron/eval/route.ts:12-13`).
- Runtime cache/CDN behaviour of the statically-eligible routes (`/llms.txt`, `/llms-full.txt`, `/feed.xml`, `/sitemap.xml`, `/robots.txt`, `/api/resume.json`). None sets `Cache-Control` and none reads the request, so they are prerenderable — but no build manifest was inspected to confirm each was actually prerendered.
- Whether the CSP was ever actually deployed as `Content-Security-Policy-Report-Only`, and the contents of the Playwright zero-violation sweep referenced at `next.config.ts:84-88`. No such audit script or spec exists in `e2e/`.
- The claim that Vercel sets HSTS by platform default (`next.config.ts:73-74`) — asserted in the comment, not verifiable from this repo.
- Whether the repo secret `SECURITY_ALERTS_TOKEN` is configured, so whether `.github/workflows/ci.yml`'s `security-alerts` job reports anything today.
- Whether the E2E CI job added in v3.4.2 is a blocking required check on `develop`/`main` — the job exists (`.github/workflows/ci.yml:68-127`) but branch-protection settings are not in the repo. **This now decides more than Playwright.** The bundle budget gate is a step inside that same job (`:110-111`), so if `e2e` is not a required check, a bundle regression reds the run and merges anyway. Nothing readable from this repo settles it.
- Whether `eslint.config.mjs`'s `globalIgnores` fully replaces `eslint-config-next`'s built-in ignores or merges with them. The comment at `:8` says "Override default ignores" and the list re-declares the four defaults, implying replace; flat-config merge semantics were not verified against the installed package.
- What generates or populates `public/static/`. It is empty and untracked; the only references are `src/lib/case-study-depth.test.ts:24` and `eslint.config.mjs:19`. No script writes to it.
- ~~The exact resolved (as-installed) versions behind the `pnpm.overrides` ranges.~~ **Now recorded.** The declaration site is `pnpm-workspace.yaml:18-28` (not a `pnpm.overrides` field any more) and `pnpm-lock.yaml:7-17` still records only the **ranges**, but v3.5.0 verified all ten pins in the resolved graph while migrating them and wrote the resolved versions down: `hono` 4.13.2 · `@hono/node-server` 1.19.17 · `ip-address` 10.5.0 · `fast-uri` 3.1.5 · `js-yaml` 4.3.1 · `postcss` 8.5.23/8.5.26 · `sharp` 0.35.3 · `body-parser` 2.3.0 · `brace-expansion` 1.1.18 and 5.0.9 (`CHANGELOG.md:190-194`). Still second-hand for this index — no resolution output was run here.
- ~~Whether the `three.js` single-copy claim reproduces (`grep -l WebGLRenderer | wc -l` must be 1).~~ **CONFIRMED, and the bundler attribution corrected.** Exactly **one** chunk in the `.next/` tree on this machine contains `WebGLRenderer`, at **897,249 B** (= 876.2 KiB, matching `next.config.ts:127-149`), and it appears in **0 of the 16** routes' first-load sets — so three.js is genuinely *lazy*, which is the stronger claim than "one copy". `domains/performance/README.md:91` states the grep test and `:92-95` states outright that every KB figure in that loop is a **Turbopack** measurement, "which the CI gate re-verifies every run" — `scripts/bundle-budget.mjs:124-127`. Still unverified in one respect: the `2036 KB → 1160 KB / −43%` before/after pair at `next.config.ts:133-144` is the code's own recorded history and was **not** re-measured here; only the post-fix side was. And what was read is a Turbopack build artifact already present on the development machine — `pnpm build` was not run *for this index* (see the last item in this list).
- Whether migrating `zod` from `^3.25.76` to v4 would break the raw-shape schema handoff to `mcp-handler`'s `registerTool` (`src/lib/mcp-tools.ts:29-36`). Both constraining peers already permit v4 (`pnpm-lock.yaml:911` for `@modelcontextprotocol/sdk@1.26.0`, `pnpm-lock.yaml:190` for `@anthropic-ai/sdk@0.116.0`), but no code or test exercises it.
- ~~Whether `@react-three/offscreen` and `@react-three/rapier` are retained intentionally for a future variant or are simply stale.~~ **Resolved by removal, not by documentation:** v3.5.0 deleted both as unused (`CHANGELOG.md:197-201`), deferred until the `pnpm-workspace.yaml` migration made lockfile regeneration safe. No file ever stated a reason for keeping them — `docs/superpowers/plans/2026-06-23-c4-r3f-physics.md` is the origin of the rapier install and does not say why it stayed.
- Whether `NEXT_PUBLIC_SKILL_TREE` or `NEXT_PUBLIC_RESUME_VARIANTS` are set in any deployed Vercel environment — only `.env.example` (both commented out) and Makefile defaults (`false`) were inspected.
- Whether `NEXT_PUBLIC_GRAPH_PHYSICS`'s default-off rationale is recorded anywhere. It is absent from `docs/configuration.md` and `.env.example`; it appears only at `.env.local:44` and `src/components/hero-graph/index.tsx:8`.
- Whether `src/lib/article-grouping.ts` is covered indirectly by any `*.dom.test.tsx` — no dedicated test file exists and the DOM suite was not read exhaustively.
- Whether the stale comments recorded above are known-stale or oversights. Only the current code/data disagreement was confirmed, not git history.
- Whether the plans/specs under `docs/superpowers/` were fully executed as written. Only two were compared against code (the flags package rename, and the resume/CSP change).
- Whether `../.aava/` and `../.claude-flow/` are ever read at runtime by anything in `sairam-dev`. No references from `src/` were found, but those toolchains were not audited.
- No test run was performed for this page beyond `npx tsc --noEmit` and `node scripts/bundle-budget.mjs` (the latter run for this page against the existing `.next/` tree: exit 0, 16 routes, largest `/` at 1,220,794 B with 64,206 B headroom, three.js lazy across 28 first-load chunks). `pnpm test`, `pnpm build`, `pnpm analyze` and `pnpm e2e` were **not** executed, so all pass/fail states quoted from the test suite are read from source, and every bundle byte quoted here is read from a build artifact rather than from a build run here.
